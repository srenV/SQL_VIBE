/**
 * Aggregations-Uebungen.
 * Enthaelt Uebungen fuer GROUP BY, SUM, COUNT, AVG, MIN, MAX und HAVING.
 */
import { makeWriteExercise, makeDebugExercise, makePredictExercise, makeSchemaExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";

export const aggregationExercises: Exercise[] = [];
resetCounter();
aggregationExercises.push(
  makeWriteExercise("agg", {
    title: "Anzahl der Zeilen in kunden",
    description: "Wie viele Kunden sind insgesamt in der Datenbank gespeichert? Gib der Ergebnisspalte den Namen `anzahl`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "shop",
    referenceQuery: `SELECT COUNT(*) AS anzahl FROM kunden;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest wissen, wie viele Datensaetze in einer Tabelle existieren — dafuer gibt es eine spezielle Zaehlfunktion.",
      "`COUNT(*)` zaehlt alle Zeilen: `SELECT COUNT(*) AS alias FROM tabelle`",
      "Kombiniere `COUNT(*)` mit der Tabelle `kunden` und vergiss nicht den Alias `anzahl`.",
    ],
    hiddenTestQuery: `SELECT COUNT(*) AS anzahl FROM kunden;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Summe von gewicht_kg",
    description: "Wie viel wiegen alle Nutzer zusammengerechnet? Gib der Ergebnisspalte den Namen `gesamt`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "fitness",
    referenceQuery: `SELECT SUM(gewicht_kg) AS gesamt FROM nutzer;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest alle Zahlenwerte einer Spalte aufaddieren — eine Aggregatfunktion erledigt das fuer dich.",
      "`SUM(spalte)` berechnet die Summe aller Werte: `SELECT SUM(spaltenname) AS alias FROM tabelle`",
      "Verwende `SUM` auf die Spalte `gewicht_kg` in der Tabelle `nutzer` mit dem Alias `gesamt`.",
    ],
    hiddenTestQuery: `SELECT SUM(gewicht_kg) AS gesamt FROM nutzer;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Durchschnitt von id",
    description: "Welcher Wert liegt im Durchschnitt zwischen allen IDs in der Uebungstabelle? Gib der Ergebnisspalte den Namen `durchschnitt`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "fitness",
    referenceQuery: `SELECT AVG(id) AS durchschnitt FROM uebungen;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest den Mittelwert aller Werte einer Spalte berechnen — dafuer gibt es eine Aggregatfunktion.",
      "`AVG(spalte)` berechnet den Durchschnitt: `SELECT AVG(spaltenname) AS alias FROM tabelle`",
      "Verwende `AVG` auf die Spalte `id` in der Tabelle `uebungen` mit dem Alias `durchschnitt`.",
    ],
    hiddenTestQuery: `SELECT AVG(id) AS durchschnitt FROM uebungen;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Maximum von id",
    description: "Wie hoch ist die hoechste vergebene ID in der Abteilungstabelle? Gib der Ergebnisspalte den Namen `maximum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "hr",
    referenceQuery: `SELECT MAX(id) AS maximum FROM abteilungen;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest den groessten Wert in einer Spalte finden — eine Aggregatfunktion liefert dir genau das.",
      "`MAX(spalte)` gibt den hoechsten Wert zurueck: `SELECT MAX(spaltenname) AS alias FROM tabelle`",
      "Verwende `MAX` auf die Spalte `id` in der Tabelle `abteilungen` mit dem Alias `maximum`.",
    ],
    hiddenTestQuery: `SELECT MAX(id) AS maximum FROM abteilungen;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Minimum von gehalt",
    description: "Wie hoch ist das niedrigste Gehalt unter allen Mitarbeitern? Gib der Ergebnisspalte den Namen `minimum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "hr",
    referenceQuery: `SELECT MIN(gehalt) AS minimum FROM mitarbeiter;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest den kleinsten Wert in einer Spalte finden — eine Aggregatfunktion liefert dir genau das.",
      "`MIN(spalte)` gibt den niedrigsten Wert zurueck: `SELECT MIN(spaltenname) AS alias FROM tabelle`",
      "Verwende `MIN` auf die Spalte `gehalt` in der Tabelle `mitarbeiter` mit dem Alias `minimum`.",
    ],
    hiddenTestQuery: `SELECT MIN(gehalt) AS minimum FROM mitarbeiter;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Gruppieren nach team",
    description: "Zeige fuer jedes Team der Agenten, wie viele Agenten ihm angehoeren. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: "tickets",
    referenceQuery: `SELECT team, COUNT(*) AS anzahl FROM agenten GROUP BY team;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Verwende `GROUP BY team` auf der Tabelle `agenten` und zaehle mit `COUNT(*)`.",
    ],
    hiddenTestQuery: `SELECT team, COUNT(*) AS anzahl FROM agenten GROUP BY team;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "HAVING in kunden",
    description: "Zeige alle Adressen, unter denen mehr als ein Kunde registriert ist, zusammen mit der jeweiligen Anzahl. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: "banking",
    referenceQuery: `SELECT adresse, COUNT(*) AS anzahl FROM kunden GROUP BY adresse HAVING COUNT(*) > 1;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest Gruppen nach ihrer Aggregation filtern — aehnlich wie WHERE, aber NACH dem Gruppieren.",
      "`HAVING` filtert Gruppen: `GROUP BY spalte HAVING COUNT(*) > grenzwert`",
      "Gruppiere `kunden` nach `adresse`, zaehle mit `COUNT(*)` und filtere mit `HAVING COUNT(*) > 1`.",
    ],
    hiddenTestQuery: `SELECT adresse, COUNT(*) AS anzahl FROM kunden GROUP BY adresse HAVING COUNT(*) > 1;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Anzahl der Zeilen in konten",
    description: "Wie viele Konten gibt es insgesamt in der Datenbank? Gib der Ergebnisspalte den Namen `anzahl`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "banking",
    referenceQuery: `SELECT COUNT(*) AS anzahl FROM konten;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest wissen, wie viele Datensaetze in einer Tabelle existieren — dafuer gibt es eine spezielle Zaehlfunktion.",
      "`COUNT(*)` zaehlt alle Zeilen: `SELECT COUNT(*) AS alias FROM tabelle`",
      "Kombiniere `COUNT(*)` mit der Tabelle `konten` und vergiss nicht den Alias `anzahl`.",
    ],
    hiddenTestQuery: `SELECT COUNT(*) AS anzahl FROM konten;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Summe von id",
    description: "Welchen Gesamtwert ergibt die Addition aller Nutzer-IDs? Gib der Ergebnisspalte den Namen `gesamt`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "streaming",
    referenceQuery: `SELECT SUM(id) AS gesamt FROM nutzer;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest alle Zahlenwerte einer Spalte aufaddieren — eine Aggregatfunktion erledigt das fuer dich.",
      "`SUM(spalte)` berechnet die Summe aller Werte: `SELECT SUM(spaltenname) AS alias FROM tabelle`",
      "Verwende `SUM` auf die Spalte `id` in der Tabelle `nutzer` mit dem Alias `gesamt`.",
    ],
    hiddenTestQuery: `SELECT SUM(id) AS gesamt FROM nutzer;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Durchschnittliche Filmdauer",
    description: "Wie lang sind die Filme im Durchschnitt in Minuten? Gib der Ergebnisspalte den Namen `durchschnitt`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "streaming",
    referenceQuery: `SELECT AVG(dauer_min) AS durchschnitt FROM filme;`,
    expectedResultText: "",
    tags: ["Aggregation", "AVG"],
    hints: [
      "Du moechtest den Mittelwert aller Werte einer Spalte berechnen — dafuer gibt es eine Aggregatfunktion.",
      "`AVG(spalte)` berechnet den Durchschnitt: `SELECT AVG(spaltenname) AS alias FROM tabelle`",
      "Verwende `AVG` auf die Spalte `dauer_min` in der Tabelle `filme` mit dem Alias `durchschnitt`.",
    ],
    hiddenTestQuery: `SELECT AVG(dauer_min) AS durchschnitt FROM filme;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Maximum von dauer_ms",
    description: "Wie lang war das laengste Event in Millisekunden? Gib der Ergebnisspalte den Namen `maximum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "logs",
    referenceQuery: `SELECT MAX(dauer_ms) AS maximum FROM events;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest den groessten Wert in einer Spalte finden — eine Aggregatfunktion liefert dir genau das.",
      "`MAX(spalte)` gibt den hoechsten Wert zurueck: `SELECT MAX(spaltenname) AS alias FROM tabelle`",
      "Verwende `MAX` auf die Spalte `dauer_ms` in der Tabelle `events` mit dem Alias `maximum`.",
    ],
    hiddenTestQuery: `SELECT MAX(dauer_ms) AS maximum FROM events;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("agg", {
    title: "Minimum von id",
    description: "Wie hoch ist die kleinste ID in der Sitzungstabelle? Gib der Ergebnisspalte den Namen `minimum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "logs",
    referenceQuery: `SELECT MIN(id) AS minimum FROM sessions;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "Du moechtest den kleinsten Wert in einer Spalte finden — eine Aggregatfunktion liefert dir genau das.",
      "`MIN(spalte)` gibt den niedrigsten Wert zurueck: `SELECT MIN(spaltenname) AS alias FROM tabelle`",
      "Verwende `MIN` auf die Spalte `id` in der Tabelle `sessions` mit dem Alias `minimum`.",
    ],
    hiddenTestQuery: `SELECT MIN(id) AS minimum FROM sessions;`,
    hiddenTestMode: "rows",
  })
);

aggregationExercises.push(
  makeWriteExercise("agg", {
    title: "Durchschnittspreis der Produkte",
    description: "Was kostet ein Produkt im Durchschnitt? Gib der Ergebnisspalte den Namen `durchschnittspreis`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT AVG(preis) AS durchschnittspreis FROM produkte;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "Du moechtest den Mittelwert aller Werte einer Spalte berechnen — dafuer gibt es eine Aggregatfunktion.",
      "`AVG(spalte)` berechnet den Durchschnitt: `SELECT AVG(spaltenname) AS alias FROM tabelle`",
      "Verwende `AVG` auf die Spalte `preis` in der Tabelle `produkte` mit dem Alias `durchschnittspreis`.",
    ],
    hiddenTestQuery: "SELECT AVG(preis) AS durchschnittspreis FROM produkte;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Teuerstes Produkt",
    description: "Was ist der hoechste Preis, den ein Produkt in der Datenbank hat? Gib der Ergebnisspalte den Namen `teuerster_preis`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT MAX(preis) AS teuerster_preis FROM produkte;",
    tags: ["Aggregation", "MAX"],
    hints: [
      "Du moechtest den groessten Wert in einer Spalte finden — eine Aggregatfunktion liefert dir genau das.",
      "`MAX(spalte)` gibt den hoechsten Wert zurueck: `SELECT MAX(spaltenname) AS alias FROM tabelle`",
      "Verwende `MAX` auf die Spalte `preis` in der Tabelle `produkte` mit dem Alias `teuerster_preis`.",
    ],
    hiddenTestQuery: "SELECT MAX(preis) AS teuerster_preis FROM produkte;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Summe aller Bestellgesamtbetraege",
    description: "Welchen Gesamtumsatz ergeben alle Bestellungen zusammen? Gib der Ergebnisspalte den Namen `summe`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT SUM(gesamtbetrag) AS summe FROM bestellungen;",
    tags: ["Aggregation", "SUM"],
    hints: [
      "Du moechtest alle Zahlenwerte einer Spalte aufaddieren — eine Aggregatfunktion erledigt das fuer dich.",
      "`SUM(spalte)` berechnet die Summe aller Werte: `SELECT SUM(spaltenname) AS alias FROM tabelle`",
      "Verwende `SUM` auf die Spalte `gesamtbetrag` in der Tabelle `bestellungen` mit dem Alias `summe`.",
    ],
    hiddenTestQuery: "SELECT SUM(gesamtbetrag) AS summe FROM bestellungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Produkte pro Kategorie",
    description: "Zeige fuer jede Kategorie, wie viele Produkte ihr zugeordnet sind. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT kategorie_id, COUNT(*) AS anzahl FROM produkte GROUP BY kategorie_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe aggregieren.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `produkte` nach `kategorie_id` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT kategorie_id, COUNT(*) AS anzahl FROM produkte GROUP BY kategorie_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Bestellungen pro Kunde",
    description: "Zeige fuer jeden Kunden, wie viele Bestellungen er aufgegeben hat. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT kunde_id, COUNT(*) AS anzahl FROM bestellungen GROUP BY kunde_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `bestellungen` nach `kunde_id` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT kunde_id, COUNT(*) AS anzahl FROM bestellungen GROUP BY kunde_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Hoechstes Gehalt pro Abteilung",
    description: "Zeige fuer jede Abteilung das hoechste Gehalt, das dort gezahlt wird. Gib der Aggregationsspalte den Alias `max_gehalt`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT abteilung_id, MAX(gehalt) AS max_gehalt FROM mitarbeiter GROUP BY abteilung_id;",
    tags: ["Aggregation", "GROUP BY", "MAX"],
    hints: [
      "Du moechtest pro Gruppe den groessten Wert einer Spalte ermitteln — kombiniere Gruppierung mit einer Aggregatfunktion.",
      "`GROUP BY spalte` gruppiert, `MAX(spalte)` gibt den Hoechstwert pro Gruppe: `SELECT gruppenspalte, MAX(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `mitarbeiter` nach `abteilung_id` und verwende `MAX(gehalt)` als `max_gehalt`.",
    ],
    hiddenTestQuery: "SELECT abteilung_id, MAX(gehalt) AS max_gehalt FROM mitarbeiter GROUP BY abteilung_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Durchschnittsgewicht der Nutzer",
    description: "Berechne das Durchschnittsgewicht aller Nutzer. Gib der Ergebnisspalte den Namen `avg_gewicht`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT AVG(gewicht_kg) AS avg_gewicht FROM nutzer;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "Du moechtest den Mittelwert aller Werte einer Spalte berechnen — dafuer gibt es eine Aggregatfunktion.",
      "`AVG(spalte)` berechnet den Durchschnitt: `SELECT AVG(spaltenname) AS alias FROM tabelle`",
      "Verwende `AVG` auf die Spalte `gewicht_kg` in der Tabelle `nutzer` mit dem Alias `avg_gewicht`.",
    ],
    hiddenTestQuery: "SELECT AVG(gewicht_kg) AS avg_gewicht FROM nutzer;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Durchschnittliche Kalorien pro Workout",
    description: "Wie viele Kalorien werden bei einem Workout durchschnittlich verbrannt? Gib der Ergebnisspalte den Namen `avg_kalorien`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT AVG(kalorien_verbrannt) AS avg_kalorien FROM workouts;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "Du moechtest den Mittelwert aller Werte einer Spalte berechnen — dafuer gibt es eine Aggregatfunktion.",
      "`AVG(spalte)` berechnet den Durchschnitt: `SELECT AVG(spaltenname) AS alias FROM tabelle`",
      "Verwende `AVG` auf die Spalte `kalorien_verbrannt` in der Tabelle `workouts` mit dem Alias `avg_kalorien`.",
    ],
    hiddenTestQuery: "SELECT AVG(kalorien_verbrannt) AS avg_kalorien FROM workouts;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Kalorien pro Nutzer summieren",
    description: "Zeige fuer jeden Nutzer, wie viele Kalorien er insgesamt bei allen Workouts verbrannt hat. Gib der Aggregationsspalte den Alias `total_kalorien`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT nutzer_id, SUM(kalorien_verbrannt) AS total_kalorien FROM workouts GROUP BY nutzer_id;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "Du moechtest pro Gruppe die Summe einer Spalte berechnen — kombiniere Gruppierung mit einer Summenfunktion.",
      "`GROUP BY spalte` gruppiert, `SUM(spalte)` summiert pro Gruppe: `SELECT gruppenspalte, SUM(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `workouts` nach `nutzer_id` und summiere `kalorien_verbrannt` als `total_kalorien`.",
    ],
    hiddenTestQuery: "SELECT nutzer_id, SUM(kalorien_verbrannt) AS total_kalorien FROM workouts GROUP BY nutzer_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Tickets pro Prioritaet",
    description: "Zeige fuer jede Prioritaetsstufe, wie viele Tickets ihr zugeordnet sind. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT prioritaet, COUNT(*) AS anzahl FROM tickets GROUP BY prioritaet;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `tickets` nach `prioritaet` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT prioritaet, COUNT(*) AS anzahl FROM tickets GROUP BY prioritaet;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Gesamtsumme der Transaktionen pro Konto",
    description: "Zeige fuer jedes Konto, welchen Gesamtbetrag alle zugehoerigen Transaktionen ergeben. Gib der Aggregationsspalte den Alias `total`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT konto_id, SUM(betrag) AS total FROM transaktionen GROUP BY konto_id;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "Du moechtest pro Gruppe die Summe einer Spalte berechnen — kombiniere Gruppierung mit einer Summenfunktion.",
      "`GROUP BY spalte` gruppiert, `SUM(spalte)` summiert pro Gruppe: `SELECT gruppenspalte, SUM(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `transaktionen` nach `konto_id` und summiere `betrag` als `total`.",
    ],
    hiddenTestQuery: "SELECT konto_id, SUM(betrag) AS total FROM transaktionen GROUP BY konto_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Filme pro Genre",
    description: "Zeige fuer jedes Genre, wie viele Filme es in der Datenbank gibt. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT genre, COUNT(*) AS anzahl FROM filme GROUP BY genre;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `filme` nach `genre` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT genre, COUNT(*) AS anzahl FROM filme GROUP BY genre;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Durchschnittsbewertung pro Film",
    description: "Zeige fuer jeden Film, wie viele Sterne er im Durchschnitt erhalten hat. Gib der Aggregationsspalte den Alias `avg_sterne`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT film_id, AVG(sterne) AS avg_sterne FROM bewertungen GROUP BY film_id;",
    tags: ["Aggregation", "GROUP BY", "AVG"],
    hints: [
      "Du moechtest pro Gruppe den Durchschnitt einer Spalte berechnen — kombiniere Gruppierung mit einer Durchschnittsfunktion.",
      "`GROUP BY spalte` gruppiert, `AVG(spalte)` berechnet den Mittelwert pro Gruppe: `SELECT gruppenspalte, AVG(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `bewertungen` nach `film_id` und verwende `AVG(sterne)` als `avg_sterne`.",
    ],
    hiddenTestQuery: "SELECT film_id, AVG(sterne) AS avg_sterne FROM bewertungen GROUP BY film_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Events pro Typ zaehlen",
    description: "Zeige fuer jeden Event-Typ, wie viele Events dieser Art aufgezeichnet wurden. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT event_typ, COUNT(*) AS anzahl FROM events GROUP BY event_typ;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `events` nach `event_typ` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT event_typ, COUNT(*) AS anzahl FROM events GROUP BY event_typ;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Kunden mit mehr als einer Bestellung (HAVING)",
    description: "Zeige fuer jeden Kunden die Zahl seiner Bestellungen, aber nur wenn er mehr als eine aufgegeben hat. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT kunde_id, COUNT(*) AS anzahl FROM bestellungen GROUP BY kunde_id HAVING COUNT(*) > 1;",
    tags: ["Aggregation", "HAVING"],
    hints: [
      "Du moechtest Gruppen nach ihrer Aggregation filtern — aehnlich wie WHERE, aber NACH dem Gruppieren.",
      "`HAVING` filtert Gruppen: `GROUP BY spalte HAVING COUNT(*) > grenzwert`",
      "Gruppiere `bestellungen` nach `kunde_id`, zaehle mit `COUNT(*)` als `anzahl` und filtere mit `HAVING COUNT(*) > 1`.",
    ],
    hiddenTestQuery: "SELECT kunde_id, COUNT(*) AS anzahl FROM bestellungen GROUP BY kunde_id HAVING COUNT(*) > 1;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Produkte teurer als der Durchschnitt",
    description: "Zeige Name und Preis aller Produkte, die teurer sind als der Durchschnitt aller Produkte.",
    difficulty: "intermediate",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, preis FROM produkte WHERE preis > (SELECT AVG(preis) FROM produkte);",
    tags: ["Aggregation", "Subquery", "AVG"],
    hints: [
      "Du moechtest Zeilen filtern, deren Wert ueber einem berechneten Durchschnitt liegt — dazu kannst du eine Unterabfrage in der WHERE-Bedingung verwenden.",
      "Eine Subquery in WHERE berechnet den Vergleichswert: `WHERE spalte > (SELECT AVG(spalte) FROM tabelle)`",
      "Verwende eine Subquery: `WHERE preis > (SELECT ...)` mit `AVG(preis)` aus der gleichen Tabelle.",
    ],
    hiddenTestQuery: "SELECT name, preis FROM produkte WHERE preis > (SELECT AVG(preis) FROM produkte);",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Mitarbeiter mit mehr als 2 Urlaubstagen",
    description: "Zeige fuer jeden Mitarbeiter seine gesamten Urlaubstage, aber nur wenn es mehr als 2 sind. Gib der Aggregationsspalte den Alias `urlaubs_tage`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT mitarbeiter_id, SUM(tage) AS urlaubs_tage FROM urlaub GROUP BY mitarbeiter_id HAVING SUM(tage) > 2;",
    tags: ["Aggregation", "HAVING", "SUM"],
    hints: [
      "Du moechtest Gruppen nach ihrer summierten Aggregation filtern — aehnlich wie WHERE, aber NACH dem Gruppieren.",
      "`HAVING` filtert Gruppen nach Aggregatwerten: `GROUP BY spalte HAVING SUM(wertspalte) > grenzwert`",
      "Gruppiere `urlaub` nach `mitarbeiter_id`, summiere `tage` als `urlaubs_tage` und filtere mit `HAVING SUM(tage) > 2`.",
    ],
    hiddenTestQuery: "SELECT mitarbeiter_id, SUM(tage) AS urlaubs_tage FROM urlaub GROUP BY mitarbeiter_id HAVING SUM(tage) > 2;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Mindestgehalt pro Abteilung",
    description: "Zeige fuer jede Abteilung das niedrigste Gehalt, das dort gezahlt wird. Gib der Aggregationsspalte den Alias `min_gehalt`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT abteilung_id, MIN(gehalt) AS min_gehalt FROM mitarbeiter GROUP BY abteilung_id;",
    tags: ["Aggregation", "GROUP BY", "MIN"],
    hints: [
      "Du moechtest pro Gruppe den kleinsten Wert einer Spalte ermitteln — kombiniere Gruppierung mit einer Aggregatfunktion.",
      "`GROUP BY spalte` gruppiert, `MIN(spalte)` gibt den Minimalwert pro Gruppe: `SELECT gruppenspalte, MIN(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `mitarbeiter` nach `abteilung_id` und verwende `MIN(gehalt)` als `min_gehalt`.",
    ],
    hiddenTestQuery: "SELECT abteilung_id, MIN(gehalt) AS min_gehalt FROM mitarbeiter GROUP BY abteilung_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Workouts pro Nutzer",
    description: "Zeige fuer jeden Nutzer, wie viele Workouts er absolviert hat. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT nutzer_id, COUNT(*) AS anzahl FROM workouts GROUP BY nutzer_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `workouts` nach `nutzer_id` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT nutzer_id, COUNT(*) AS anzahl FROM workouts GROUP BY nutzer_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Fehler pro Schweregrad",
    description: "Zeige fuer jeden Schweregrad, wie viele Fehler dieser Kategorie aufgetreten sind. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT schweregrad, COUNT(*) AS anzahl FROM fehler GROUP BY schweregrad;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `fehler` nach `schweregrad` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT schweregrad, COUNT(*) AS anzahl FROM fehler GROUP BY schweregrad;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Anzahl der Konten pro Typ",
    description: "Zeige fuer jeden Kontotyp, wie viele Konten es davon gibt. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT typ, COUNT(*) AS anzahl FROM konten GROUP BY typ;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `konten` nach `typ` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT typ, COUNT(*) AS anzahl FROM konten GROUP BY typ;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Gesamtumsatz pro Zahlungsart",
    description: "Zeige fuer jede Zahlungsart, welchen Gesamtbetrag alle damit getaetigten Zahlungen ergeben. Gib der Aggregationsspalte den Alias `total`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT zahlungsmittel, SUM(betrag) AS total FROM zahlungen GROUP BY zahlungsmittel;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "Du moechtest pro Gruppe die Summe einer Spalte berechnen — kombiniere Gruppierung mit einer Summenfunktion.",
      "`GROUP BY spalte` gruppiert, `SUM(spalte)` summiert pro Gruppe: `SELECT gruppenspalte, SUM(wertspalte) AS alias FROM tabelle GROUP BY gruppenspalte`",
      "Gruppiere `zahlungen` nach `zahlungsmittel` und summiere `betrag` als `total`.",
    ],
    hiddenTestQuery: "SELECT zahlungsmittel, SUM(betrag) AS total FROM zahlungen GROUP BY zahlungsmittel;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Tickets pro Status",
    description: "Zeige fuer jeden Status, wie viele Tickets sich aktuell darin befinden. Gib der Aggregationsspalte den Alias `anzahl`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT status, COUNT(*) AS anzahl FROM tickets GROUP BY status;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "Du moechtest die Daten anhand einer Spalte in Gruppen aufteilen und pro Gruppe zaehlen.",
      "`GROUP BY spalte` gruppiert die Zeilen, `COUNT(*)` zaehlt pro Gruppe: `SELECT spalte, COUNT(*) AS anzahl FROM tabelle GROUP BY spalte`",
      "Gruppiere `tickets` nach `status` und zaehle mit `COUNT(*)` als `anzahl`.",
    ],
    hiddenTestQuery: "SELECT status, COUNT(*) AS anzahl FROM tickets GROUP BY status;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Bewertungen hoeher als Durchschnitt (Subquery)",
    description: "Zeige alle Bewertungen, die besser sind als die durchschnittliche Bewertung aller Filme.",
    difficulty: "intermediate",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT * FROM bewertungen WHERE sterne > (SELECT AVG(sterne) FROM bewertungen);",
    tags: ["Aggregation", "Subquery", "AVG"],
    hints: [
      "Du moechtest Zeilen filtern, deren Wert ueber einem berechneten Durchschnitt liegt — dazu kannst du eine Unterabfrage in der WHERE-Bedingung verwenden.",
      "Eine Subquery in WHERE berechnet den Vergleichswert: `WHERE spalte > (SELECT AVG(spalte) FROM tabelle)`",
      "Verwende eine Subquery: `WHERE sterne > (SELECT ...)` mit `AVG(sterne)` aus der gleichen Tabelle.",
    ],
    hiddenTestQuery: "SELECT * FROM bewertungen WHERE sterne > (SELECT AVG(sterne) FROM bewertungen);",
    hiddenTestMode: "rows",
  })
);
