/**
 * JOIN exercises (English).
 * Contains exercises for INNER JOIN, LEFT JOIN, and RIGHT JOIN.
 */
import { makeWriteExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";

export const joinExercisesEn: Exercise[] = [];
resetCounter();
joinExercisesEn.push(
  makeWriteExercise("joi", {
    title: "Join customers and orders",
    description: "Join `customers` and `orders` with an INNER JOIN. Output the customer name and the order total amount.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, total_amount FROM customers INNER JOIN orders ON customers.id = orders.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM customers INNER JOIN orders ON customers.id = orders.customer_id`",
    ],
    hiddenTestQuery: `SELECT name, total_amount FROM customers INNER JOIN orders ON customers.id = orders.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between customers and orders",
    description: "Output the name of each customer and the total amount of their orders. Customers without orders should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, total_amount FROM customers LEFT JOIN orders ON customers.id = orders.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.id = right.foreign_key`",
      "Specifically: `FROM customers LEFT JOIN orders ON customers.id = orders.customer_id`",
    ],
    hiddenTestQuery: `SELECT name, total_amount FROM customers LEFT JOIN orders ON customers.id = orders.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between customers and orders",
    description: "Output the customer name and the total amount of each order. Orders without an associated customer entry should also appear with their total amount.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, total_amount FROM customers RIGHT JOIN orders ON customers.id = orders.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.id = right.foreign_key`",
      "Specifically: `FROM customers RIGHT JOIN orders ON customers.id = orders.customer_id`",
    ],
    hiddenTestQuery: `SELECT name, total_amount FROM customers RIGHT JOIN orders ON customers.id = orders.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count orders per customer (JOIN + GROUP BY)",
    description: "Count how many orders each customer has placed. Output the customer ID and the number of orders per customer.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN orders ON customers.id = orders.customer_id GROUP BY customers.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN orders ON customers.id = orders.customer_id GROUP BY customers.id`",
    ],
    hiddenTestQuery: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN orders ON customers.id = orders.customer_id GROUP BY customers.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join products and categories",
    description: "Join `products` and `categories` with an INNER JOIN. Output the product name and the category name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT products.name, categories.name FROM products INNER JOIN categories ON products.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.foreign_key = table2.key`",
      "Specifically: `FROM products INNER JOIN categories ON products.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT products.name, categories.name FROM products INNER JOIN categories ON products.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between products and categories",
    description: "Output the name of each product and the name of its category. Products without an assigned category should also appear with their product name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT products.name, categories.name FROM products LEFT JOIN categories ON products.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM products LEFT JOIN categories ON products.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT products.name, categories.name FROM products LEFT JOIN categories ON products.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between products and categories",
    description: "Output the product name and the category name. Categories without an assigned product should also appear with their category name.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT products.name, categories.name FROM products RIGHT JOIN categories ON products.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM products RIGHT JOIN categories ON products.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT products.name, categories.name FROM products RIGHT JOIN categories ON products.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count categories per product (JOIN + GROUP BY)",
    description: "Count how many categories are assigned to each product. Output the product ID and the number of categories per product.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT products.id, COUNT(*) AS count FROM products INNER JOIN categories ON products.category_id = categories.id GROUP BY products.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT products.id, COUNT(*) AS count FROM products INNER JOIN categories ON products.category_id = categories.id GROUP BY products.id`",
    ],
    hiddenTestQuery: `SELECT products.id, COUNT(*) AS count FROM products INNER JOIN categories ON products.category_id = categories.id GROUP BY products.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join orders and payments",
    description: "Join `orders` and `payments` with an INNER JOIN. Output the order status and the payment amount.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, amount FROM orders INNER JOIN payments ON orders.id = payments.order_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM orders INNER JOIN payments ON orders.id = payments.order_id`",
    ],
    hiddenTestQuery: `SELECT status, amount FROM orders INNER JOIN payments ON orders.id = payments.order_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between orders and payments",
    description: "Output the status of each order and the amount of the associated payment. Orders without a payment should also appear with their status.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, amount FROM orders LEFT JOIN payments ON orders.id = payments.order_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM orders LEFT JOIN payments ON orders.id = payments.order_id`",
    ],
    hiddenTestQuery: `SELECT status, amount FROM orders LEFT JOIN payments ON orders.id = payments.order_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between orders and payments",
    description: "Output the order status and the payment amount. Payments without an associated order should also appear with their amount.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, amount FROM orders RIGHT JOIN payments ON orders.id = payments.order_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM orders RIGHT JOIN payments ON orders.id = payments.order_id`",
    ],
    hiddenTestQuery: `SELECT status, amount FROM orders RIGHT JOIN payments ON orders.id = payments.order_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count payments per order (JOIN + GROUP BY)",
    description: "Count how many payments are assigned to each order. Output the order ID and the number of payments per order.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT orders.id, COUNT(*) AS count FROM orders INNER JOIN payments ON orders.id = payments.order_id GROUP BY orders.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT orders.id, COUNT(*) AS count FROM orders INNER JOIN payments ON orders.id = payments.order_id GROUP BY orders.id`",
    ],
    hiddenTestQuery: `SELECT orders.id, COUNT(*) AS count FROM orders INNER JOIN payments ON orders.id = payments.order_id GROUP BY orders.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join users and workouts",
    description: "Join `users` and `workouts` with an INNER JOIN. Output the user name and the duration in minutes.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT users.name, duration_min FROM users INNER JOIN workouts ON users.id = workouts.user_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM users INNER JOIN workouts ON users.id = workouts.user_id`",
    ],
    hiddenTestQuery: `SELECT users.name, duration_min FROM users INNER JOIN workouts ON users.id = workouts.user_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between users and workouts",
    description: "Output the name of each user and the duration of their workouts in minutes. Users without workouts should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT users.name, duration_min FROM users LEFT JOIN workouts ON users.id = workouts.user_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM users LEFT JOIN workouts ON users.id = workouts.user_id`",
    ],
    hiddenTestQuery: `SELECT users.name, duration_min FROM users LEFT JOIN workouts ON users.id = workouts.user_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between users and workouts",
    description: "Output the user name and the workout duration in minutes. Workouts without an assigned user should also appear with their duration.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT users.name, duration_min FROM users RIGHT JOIN workouts ON users.id = workouts.user_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM users RIGHT JOIN workouts ON users.id = workouts.user_id`",
    ],
    hiddenTestQuery: `SELECT users.name, duration_min FROM users RIGHT JOIN workouts ON users.id = workouts.user_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count workouts per user (JOIN + GROUP BY)",
    description: "Count how many workouts each user has completed. Output the user ID and the number of workouts per user.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT users.id, COUNT(*) AS count FROM users INNER JOIN workouts ON users.id = workouts.user_id GROUP BY users.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT users.id, COUNT(*) AS count FROM users INNER JOIN workouts ON users.id = workouts.user_id GROUP BY users.id`",
    ],
    hiddenTestQuery: `SELECT users.id, COUNT(*) AS count FROM users INNER JOIN workouts ON users.id = workouts.user_id GROUP BY users.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join workouts and sets",
    description: "Join `workouts` and `sets` with an INNER JOIN. Output the workout date and the number of repetitions.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT date, repetitions FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id`",
    ],
    hiddenTestQuery: `SELECT date, repetitions FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between workouts and sets",
    description: "Output the date of each workout and the repetition counts of the associated sets. Workouts without sets should also appear with their date.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT date, repetitions FROM workouts LEFT JOIN sets ON workouts.id = sets.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM workouts LEFT JOIN sets ON workouts.id = sets.workout_id`",
    ],
    hiddenTestQuery: `SELECT date, repetitions FROM workouts LEFT JOIN sets ON workouts.id = sets.workout_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between workouts and sets",
    description: "Output the workout date and the repetition count of each set. Sets without an assigned workout should also appear with their repetitions.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT date, repetitions FROM workouts RIGHT JOIN sets ON workouts.id = sets.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM workouts RIGHT JOIN sets ON workouts.id = sets.workout_id`",
    ],
    hiddenTestQuery: `SELECT date, repetitions FROM workouts RIGHT JOIN sets ON workouts.id = sets.workout_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count sets per workout (JOIN + GROUP BY)",
    description: "Count how many sets each workout contains. Output the workout ID and the number of sets per workout.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT workouts.id, COUNT(*) AS count FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id GROUP BY workouts.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT workouts.id, COUNT(*) AS count FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id GROUP BY workouts.id`",
    ],
    hiddenTestQuery: `SELECT workouts.id, COUNT(*) AS count FROM workouts INNER JOIN sets ON workouts.id = sets.workout_id GROUP BY workouts.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join employees and departments",
    description: "Join `employees` and `departments` with an INNER JOIN. Output the employee name and the department name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT employees.name, departments.name FROM employees INNER JOIN departments ON employees.department_id = departments.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.foreign_key = table2.key`",
      "Specifically: `FROM employees INNER JOIN departments ON employees.department_id = departments.id`",
    ],
    hiddenTestQuery: `SELECT employees.name, departments.name FROM employees INNER JOIN departments ON employees.department_id = departments.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between employees and departments",
    description: "Output the name of each employee and the name of their department. Employees without an assigned department should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT employees.name, departments.name FROM employees LEFT JOIN departments ON employees.department_id = departments.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM employees LEFT JOIN departments ON employees.department_id = departments.id`",
    ],
    hiddenTestQuery: `SELECT employees.name, departments.name FROM employees LEFT JOIN departments ON employees.department_id = departments.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between employees and departments",
    description: "Output the employee name and the department name. Departments without assigned employees should also appear with their department name.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT employees.name, departments.name FROM employees RIGHT JOIN departments ON employees.department_id = departments.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM employees RIGHT JOIN departments ON employees.department_id = departments.id`",
    ],
    hiddenTestQuery: `SELECT employees.name, departments.name FROM employees RIGHT JOIN departments ON employees.department_id = departments.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count departments per employee (JOIN + GROUP BY)",
    description: "Count how many departments each employee belongs to. Output the employee ID and the number of departments per employee.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN departments ON employees.department_id = departments.id GROUP BY employees.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN departments ON employees.department_id = departments.id GROUP BY employees.id`",
    ],
    hiddenTestQuery: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN departments ON employees.department_id = departments.id GROUP BY employees.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join employees and vacation",
    description: "Join `employees` and `vacation` with an INNER JOIN. Output the employee name and the number of vacation days.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, days FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id`",
    ],
    hiddenTestQuery: `SELECT name, days FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between employees and vacation",
    description: "Output the name of each employee and the number of their booked vacation days. Employees without a vacation entry should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, days FROM employees LEFT JOIN vacation ON employees.id = vacation.employee_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM employees LEFT JOIN vacation ON employees.id = vacation.employee_id`",
    ],
    hiddenTestQuery: `SELECT name, days FROM employees LEFT JOIN vacation ON employees.id = vacation.employee_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between employees and vacation",
    description: "Output the employee name and the number of vacation days. Vacation entries without an associated employee should also appear with their day count.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, days FROM employees RIGHT JOIN vacation ON employees.id = vacation.employee_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM employees RIGHT JOIN vacation ON employees.id = vacation.employee_id`",
    ],
    hiddenTestQuery: `SELECT name, days FROM employees RIGHT JOIN vacation ON employees.id = vacation.employee_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count vacation entries per employee (JOIN + GROUP BY)",
    description: "Count how many vacation entries each employee has. Output the employee ID and the number of entries per employee.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id GROUP BY employees.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id GROUP BY employees.id`",
    ],
    hiddenTestQuery: `SELECT employees.id, COUNT(*) AS count FROM employees INNER JOIN vacation ON employees.id = vacation.employee_id GROUP BY employees.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join tickets and agents",
    description: "Join `tickets` and `agents` with an INNER JOIN. Output the ticket title and the agent name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, agents.name FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.foreign_key = table2.key`",
      "Specifically: `FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, agents.name FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between tickets and agents",
    description: "Output the title of each ticket and the name of the responsible agent. Tickets without an assigned agent should also appear with their title.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, agents.name FROM tickets LEFT JOIN agents ON tickets.agent_id = agents.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM tickets LEFT JOIN agents ON tickets.agent_id = agents.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, agents.name FROM tickets LEFT JOIN agents ON tickets.agent_id = agents.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between tickets and agents",
    description: "Output the ticket title and the agent name. Agents without assigned tickets should also appear with their name.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, agents.name FROM tickets RIGHT JOIN agents ON tickets.agent_id = agents.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM tickets RIGHT JOIN agents ON tickets.agent_id = agents.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, agents.name FROM tickets RIGHT JOIN agents ON tickets.agent_id = agents.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count agents per ticket (JOIN + GROUP BY)",
    description: "Count how many agents are assigned to each ticket. Output the ticket ID and the number of agents per ticket.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id GROUP BY tickets.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id GROUP BY tickets.id`",
    ],
    hiddenTestQuery: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN agents ON tickets.agent_id = agents.id GROUP BY tickets.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join tickets and categories",
    description: "Join `tickets` and `categories` with an INNER JOIN. Output the ticket title and the category name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, categories.name FROM tickets INNER JOIN categories ON tickets.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.foreign_key = table2.key`",
      "Specifically: `FROM tickets INNER JOIN categories ON tickets.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, categories.name FROM tickets INNER JOIN categories ON tickets.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between tickets and categories",
    description: "Output the title of each ticket and the name of its category. Tickets without a category should also appear with their title.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, categories.name FROM tickets LEFT JOIN categories ON tickets.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM tickets LEFT JOIN categories ON tickets.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, categories.name FROM tickets LEFT JOIN categories ON tickets.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between tickets and categories",
    description: "Output the ticket title and the category name. Categories without assigned tickets should also appear with their category name.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.title, categories.name FROM tickets RIGHT JOIN categories ON tickets.category_id = categories.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.foreign_key = right.key`",
      "Specifically: `FROM tickets RIGHT JOIN categories ON tickets.category_id = categories.id`",
    ],
    hiddenTestQuery: `SELECT tickets.title, categories.name FROM tickets RIGHT JOIN categories ON tickets.category_id = categories.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count categories per ticket (JOIN + GROUP BY)",
    description: "Count how many categories are assigned to each ticket. Output the ticket ID and the number of categories per ticket.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN categories ON tickets.category_id = categories.id GROUP BY tickets.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN categories ON tickets.category_id = categories.id GROUP BY tickets.id`",
    ],
    hiddenTestQuery: `SELECT tickets.id, COUNT(*) AS count FROM tickets INNER JOIN categories ON tickets.category_id = categories.id GROUP BY tickets.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join customers and accounts",
    description: "Join `customers` and `accounts` with an INNER JOIN. Output the customer name and the account balance.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT customers.name, balance FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id`",
    ],
    hiddenTestQuery: `SELECT customers.name, balance FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between customers and accounts",
    description: "Output the name of each customer and the balance of their accounts. Customers without an account should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT customers.name, balance FROM customers LEFT JOIN accounts ON customers.id = accounts.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM customers LEFT JOIN accounts ON customers.id = accounts.customer_id`",
    ],
    hiddenTestQuery: `SELECT customers.name, balance FROM customers LEFT JOIN accounts ON customers.id = accounts.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between customers and accounts",
    description: "Output the customer name and the account balance. Accounts without an associated customer should also appear with their balance.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT customers.name, balance FROM customers RIGHT JOIN accounts ON customers.id = accounts.customer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM customers RIGHT JOIN accounts ON customers.id = accounts.customer_id`",
    ],
    hiddenTestQuery: `SELECT customers.name, balance FROM customers RIGHT JOIN accounts ON customers.id = accounts.customer_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count accounts per customer (JOIN + GROUP BY)",
    description: "Count how many accounts each customer owns. Output the customer ID and the number of accounts per customer.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id GROUP BY customers.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id GROUP BY customers.id`",
    ],
    hiddenTestQuery: `SELECT customers.id, COUNT(*) AS count FROM customers INNER JOIN accounts ON customers.id = accounts.customer_id GROUP BY customers.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join accounts and transactions",
    description: "Join `accounts` and `transactions` with an INNER JOIN. Output the account number and the transaction amount.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT account_number, amount FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id`",
    ],
    hiddenTestQuery: `SELECT account_number, amount FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between accounts and transactions",
    description: "Output the account number of each account and the amount of the associated transactions. Accounts without transactions should also appear with their account number.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT account_number, amount FROM accounts LEFT JOIN transactions ON accounts.id = transactions.account_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM accounts LEFT JOIN transactions ON accounts.id = transactions.account_id`",
    ],
    hiddenTestQuery: `SELECT account_number, amount FROM accounts LEFT JOIN transactions ON accounts.id = transactions.account_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "RIGHT JOIN between accounts and transactions",
    description: "Output the account number and the transaction amount. Transactions without an assigned account should also appear with their amount.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT account_number, amount FROM accounts RIGHT JOIN transactions ON accounts.id = transactions.account_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A RIGHT JOIN returns ALL rows from the right (second) table — the mirror image of LEFT JOIN. Missing values on the left side are shown as NULL.",
      "Syntax: `FROM left_table RIGHT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM accounts RIGHT JOIN transactions ON accounts.id = transactions.account_id`",
    ],
    hiddenTestQuery: `SELECT account_number, amount FROM accounts RIGHT JOIN transactions ON accounts.id = transactions.account_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count transactions per account (JOIN + GROUP BY)",
    description: "Count how many transactions each account has. Output the account ID and the number of transactions per account.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT accounts.id, COUNT(*) AS count FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id GROUP BY accounts.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT accounts.id, COUNT(*) AS count FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id GROUP BY accounts.id`",
    ],
    hiddenTestQuery: `SELECT accounts.id, COUNT(*) AS count FROM accounts INNER JOIN transactions ON accounts.id = transactions.account_id GROUP BY accounts.id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join users and watch history",
    description: "Join `users` and `watch_history` with an INNER JOIN. Output the user name and the progress percentage.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT users.name, progress_percent FROM users INNER JOIN watch_history ON users.id = watch_history.user_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM users INNER JOIN watch_history ON users.id = watch_history.user_id`",
    ],
    hiddenTestQuery: `SELECT users.name, progress_percent FROM users INNER JOIN watch_history ON users.id = watch_history.user_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between users and watch history",
    description: "Output the name of each user and the progress percentage of their watched content. Users without watch history should also appear with their name.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT users.name, progress_percent FROM users LEFT JOIN watch_history ON users.id = watch_history.user_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM users LEFT JOIN watch_history ON users.id = watch_history.user_id`",
    ],
    hiddenTestQuery: `SELECT users.name, progress_percent FROM users LEFT JOIN watch_history ON users.id = watch_history.user_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Join movies and reviews",
    description: "Join `movies` and `reviews` with an INNER JOIN. Output the movie title and the star rating.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT movies.title, stars FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "An INNER JOIN connects two tables and only returns rows that have a match in BOTH tables.",
      "Syntax: `FROM table1 INNER JOIN table2 ON table1.key = table2.foreign_key`",
      "Specifically: `FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id`",
    ],
    hiddenTestQuery: `SELECT movies.title, stars FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "LEFT JOIN between movies and reviews",
    description: "Output the title of each movie and the star ratings of the associated reviews. Movies without reviews should also appear with their title.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT movies.title, stars FROM movies LEFT JOIN reviews ON movies.id = reviews.movie_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "A LEFT JOIN returns ALL rows from the left (first) table — even if there is no match in the right table. Missing values are shown as NULL.",
      "Syntax: `FROM left_table LEFT JOIN right_table ON left.key = right.foreign_key`",
      "Specifically: `FROM movies LEFT JOIN reviews ON movies.id = reviews.movie_id`",
    ],
    hiddenTestQuery: `SELECT movies.title, stars FROM movies LEFT JOIN reviews ON movies.id = reviews.movie_id;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("joi", {
    title: "Count reviews per movie (JOIN + GROUP BY)",
    description: "Count how many reviews each movie has received. Output the movie ID and the number of reviews per movie.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT movies.id, COUNT(*) AS count FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id GROUP BY movies.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "First join the tables with a JOIN, then group the result rows by a column to count per group.",
      "Combine JOIN with `GROUP BY column` and `COUNT(*) AS alias`.",
      "Structure: `SELECT movies.id, COUNT(*) AS count FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id GROUP BY movies.id`",
    ],
    hiddenTestQuery: `SELECT movies.id, COUNT(*) AS count FROM movies INNER JOIN reviews ON movies.id = reviews.movie_id GROUP BY movies.id;`,
    hiddenTestMode: "rows",
  })
);
