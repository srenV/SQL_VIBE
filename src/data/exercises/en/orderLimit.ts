/**
 * ORDER BY and LIMIT exercises (English).
 * Contains exercises for sorting and result set limiting.
 */
import { makeWriteExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";

export const orderLimitExercisesEn: Exercise[] = [];
resetCounter();
orderLimitExercisesEn.push(
  makeWriteExercise("ord", {
    title: "Sort customers by city",
    description: "Show all customers sorted alphabetically by their city, from A to Z.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "shop",
    referenceQuery: `SELECT * FROM customers ORDER BY city ASC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results by a column — smallest or earliest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Sort `customers` ascending by `city` with `ORDER BY city ASC`.",
    ],
    hiddenTestQuery: `SELECT * FROM customers ORDER BY city ASC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Sort users descending",
    description: "Show all users ordered so that the highest ID numbers appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "fitness",
    referenceQuery: `SELECT * FROM users ORDER BY id DESC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `users` descending by `id` with `ORDER BY id DESC`.",
    ],
    hiddenTestQuery: `SELECT * FROM users ORDER BY id DESC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 5 from exercises",
    description: "Show only the 5 exercises whose category name is alphabetically last.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "fitness",
    referenceQuery: `SELECT * FROM exercises ORDER BY category DESC LIMIT 5;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `exercises` descending by `category` and limit to 5 rows with `LIMIT 5`.",
    ],
    hiddenTestQuery: `SELECT * FROM exercises ORDER BY category DESC LIMIT 5;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "First 3 entries from departments",
    description: "Show the 3 departments with the lowest budget.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "hr",
    referenceQuery: `SELECT * FROM departments ORDER BY budget ASC LIMIT 3;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column ASC` with `LIMIT n` at the end of the query.",
      "Sort `departments` ascending by `budget` and limit to 3 rows with `LIMIT 3`.",
    ],
    hiddenTestQuery: `SELECT * FROM departments ORDER BY budget ASC LIMIT 3;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Sort employees by position",
    description: "Show all employees sorted alphabetically by their position, from A to Z.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "hr",
    referenceQuery: `SELECT * FROM employees ORDER BY position ASC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results by a column — smallest or alphabetically earliest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Sort `employees` ascending by `position` with `ORDER BY position ASC`.",
    ],
    hiddenTestQuery: `SELECT * FROM employees ORDER BY position ASC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Sort agents descending",
    description: "Show all agents ordered so that the active entries appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "tickets",
    referenceQuery: `SELECT * FROM agents ORDER BY active DESC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `agents` descending by `active` with `ORDER BY active DESC`.",
    ],
    hiddenTestQuery: `SELECT * FROM agents ORDER BY active DESC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 5 from customers",
    description: "Show the 5 youngest customers by birth date.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "banking",
    referenceQuery: `SELECT * FROM customers ORDER BY birth_date DESC LIMIT 5;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `customers` descending by `birth_date` and limit to 5 rows with `LIMIT 5`.",
    ],
    hiddenTestQuery: `SELECT * FROM customers ORDER BY birth_date DESC LIMIT 5;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "First 3 entries from accounts",
    description: "Show the 3 accounts with the lowest customer numbers.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "banking",
    referenceQuery: `SELECT * FROM accounts ORDER BY customer_id ASC LIMIT 3;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column ASC` with `LIMIT n` at the end of the query.",
      "Sort `accounts` ascending by `customer_id` and limit to 3 rows with `LIMIT 3`.",
    ],
    hiddenTestQuery: `SELECT * FROM accounts ORDER BY customer_id ASC LIMIT 3;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Sort users by name",
    description: "Show all users in alphabetical order by their name.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "streaming",
    referenceQuery: `SELECT * FROM users ORDER BY name ASC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results by a column — alphabetically smallest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Sort `users` ascending by `name` with `ORDER BY name ASC`.",
    ],
    hiddenTestQuery: `SELECT * FROM users ORDER BY name ASC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Sort movies descending",
    description: "Show all movies ordered so that the newest production years appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "streaming",
    referenceQuery: `SELECT * FROM movies ORDER BY year DESC;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `movies` descending by `year` with `ORDER BY year DESC`.",
    ],
    hiddenTestQuery: `SELECT * FROM movies ORDER BY year DESC;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 5 from events",
    description: "Show the 5 events with the highest session numbers.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "logs",
    referenceQuery: `SELECT * FROM events ORDER BY session_id DESC LIMIT 5;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `events` descending by `session_id` and limit to 5 rows with `LIMIT 5`.",
    ],
    hiddenTestQuery: `SELECT * FROM events ORDER BY session_id DESC LIMIT 5;`,
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "First 3 entries from sessions",
    description: "Show the 3 sessions whose browser name is alphabetically first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: "logs",
    referenceQuery: `SELECT * FROM sessions ORDER BY browser ASC LIMIT 3;`,
    expectedResultText: "",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column ASC` with `LIMIT n` at the end of the query.",
      "Sort `sessions` ascending by `browser` and limit to 3 rows with `LIMIT 3`.",
    ],
    hiddenTestQuery: `SELECT * FROM sessions ORDER BY browser ASC LIMIT 3;`,
    hiddenTestMode: "exact",
  })
);

orderLimitExercisesEn.push(
  makeWriteExercise("ord", {
    title: "Products sorted by price",
    description: "Show the name and price of all products from cheapest to most expensive.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products ORDER BY price ASC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results by a column — smallest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Select `name` and `price` from `products` and sort ascending by `price`.",
    ],
    hiddenTestQuery: "SELECT name, price FROM products ORDER BY price ASC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 3 most expensive products",
    description: "Show the name and price of the three most expensive products, the most expensive first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products ORDER BY price DESC LIMIT 3;",
    tags: ["ORDER BY", "LIMIT", "Sorting"],
    hints: [
      "You want to sort descending first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Select `name` and `price` from `products`, sort descending by `price` and limit to 3 rows.",
    ],
    hiddenTestQuery: "SELECT name, price FROM products ORDER BY price DESC LIMIT 3;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Orders sorted by date",
    description: "Show all orders chronologically, starting with the oldest.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM orders ORDER BY date ASC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results by a column — oldest entries first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Sort `orders` ascending by `date` with `ORDER BY date ASC`.",
    ],
    hiddenTestQuery: "SELECT * FROM orders ORDER BY date ASC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Employees sorted by salary",
    description: "Show the name and salary of all employees, the highest-paid first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary FROM employees ORDER BY salary DESC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Select `name` and `salary` from `employees` and sort descending by `salary`.",
    ],
    hiddenTestQuery: "SELECT name, salary FROM employees ORDER BY salary DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 3 highest-earning employees",
    description: "Show the name and salary of the three employees with the highest salary.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary FROM employees ORDER BY salary DESC LIMIT 3;",
    tags: ["ORDER BY", "LIMIT"],
    hints: [
      "You want to sort descending first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Select `name` and `salary` from `employees`, sort descending by `salary` and limit to 3 rows.",
    ],
    hiddenTestQuery: "SELECT name, salary FROM employees ORDER BY salary DESC LIMIT 3;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Exercises sorted by muscle group",
    description: "Show all exercises sorted alphabetically by muscle group.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM exercises ORDER BY muscle_group ASC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results by a column — alphabetically smallest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Sort `exercises` ascending by `muscle_group` with `ORDER BY muscle_group ASC`.",
    ],
    hiddenTestQuery: "SELECT * FROM exercises ORDER BY muscle_group ASC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Workouts sorted by duration",
    description: "Show all workouts ordered so that the longest ones appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM workouts ORDER BY duration_min DESC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `workouts` descending by `duration_min` with `ORDER BY duration_min DESC`.",
    ],
    hiddenTestQuery: "SELECT * FROM workouts ORDER BY duration_min DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Tickets sorted by priority",
    description: "Show the title and priority of all tickets, the lowest priority first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT title, priority FROM tickets ORDER BY priority ASC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results by a column — smallest values first.",
      "`ORDER BY column ASC` sorts in ascending order (ASC is the default and can be omitted).",
      "Select `title` and `priority` from `tickets` and sort ascending by `priority`.",
    ],
    hiddenTestQuery: "SELECT title, priority FROM tickets ORDER BY priority ASC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Newest 5 tickets",
    description: "Show the 5 most recently created tickets, the newest first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT * FROM tickets ORDER BY created_at DESC LIMIT 5;",
    tags: ["ORDER BY", "LIMIT"],
    hints: [
      "You want to sort descending first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `tickets` descending by `created_at` and limit to 5 rows with `LIMIT 5`.",
    ],
    hiddenTestQuery: "SELECT * FROM tickets ORDER BY created_at DESC LIMIT 5;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Transactions sorted by amount",
    description: "Show all transactions ordered so that the highest amounts appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM transactions ORDER BY amount DESC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `transactions` descending by `amount` with `ORDER BY amount DESC`.",
    ],
    hiddenTestQuery: "SELECT * FROM transactions ORDER BY amount DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Accounts sorted by balance",
    description: "Show all accounts ordered so that the highest balance appears first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM accounts ORDER BY balance DESC;",
    tags: ["ORDER BY", "Sorting"],
    hints: [
      "You want to sort the results in descending order — largest values first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `accounts` descending by `balance` with `ORDER BY balance DESC`.",
    ],
    hiddenTestQuery: "SELECT * FROM accounts ORDER BY balance DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Top 5 highest balances",
    description: "Output the 5 accounts with the highest balance.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM accounts ORDER BY balance DESC LIMIT 5;",
    tags: ["ORDER BY", "LIMIT"],
    hints: [
      "You want to sort descending first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `accounts` descending by `balance` and limit to 5 rows with `LIMIT 5`.",
    ],
    hiddenTestQuery: "SELECT * FROM accounts ORDER BY balance DESC LIMIT 5;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Movies sorted by year and duration",
    description: "Show the title, release year, and runtime of all movies, ordered chronologically and, for the same year, the longest ones first.",
    difficulty: "junior",
    category: "ORDER BY / LIMIT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, year, duration_min FROM movies ORDER BY year ASC, duration_min DESC;",
    tags: ["ORDER BY", "Multi-column"],
    hints: [
      "You want to sort by multiple columns — the first column is the primary criterion, the second breaks ties.",
      "Separate multiple sort columns with commas: `ORDER BY column1 ASC, column2 DESC`",
      "Select `title`, `year`, `duration_min` from `movies` and sort by `year ASC, duration_min DESC`.",
    ],
    hiddenTestQuery: "SELECT title, year, duration_min FROM movies ORDER BY year ASC, duration_min DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Errors sorted by severity",
    description: "Show all errors, starting with the lowest severity, and for the same severity, by error code ascending.",
    difficulty: "junior",
    category: "ORDER BY / LIMIT",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT * FROM errors ORDER BY severity ASC, error_code ASC;",
    tags: ["ORDER BY", "Multi-column"],
    hints: [
      "You want to sort by multiple columns — the first column is the primary criterion, the second breaks ties.",
      "Separate multiple sort columns with commas: `ORDER BY column1 ASC, column2 ASC`",
      "Sort `errors` ascending by `severity` and, in case of ties, by `error_code` ascending.",
    ],
    hiddenTestQuery: "SELECT * FROM errors ORDER BY severity ASC, error_code ASC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Customers sorted by registration date",
    description: "Show the name and registration date of customers, the most recently registered first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, registered_on FROM customers ORDER BY registered_on DESC;",
    tags: ["ORDER BY"],
    hints: [
      "You want to sort the results in descending order — newest entries first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Select `name` and `registered_on` from `customers` and sort descending by `registered_on`.",
    ],
    hiddenTestQuery: "SELECT name, registered_on FROM customers ORDER BY registered_on DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Applications sorted by date",
    description: "Show all applications ordered so that the newest ones appear first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT * FROM applications ORDER BY application_date DESC;",
    tags: ["ORDER BY"],
    hints: [
      "You want to sort the results in descending order — newest entries first.",
      "`ORDER BY column DESC` sorts in descending order.",
      "Sort `applications` descending by `application_date` with `ORDER BY application_date DESC`.",
    ],
    hiddenTestQuery: "SELECT * FROM applications ORDER BY application_date DESC;",
    hiddenTestMode: "exact",
  }),
  makeWriteExercise("ord", {
    title: "Comments sorted by creation date",
    description: "Show the 10 most recently written comments, the newest first.",
    difficulty: "beginner",
    category: "ORDER BY / LIMIT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT * FROM comments ORDER BY created_at DESC LIMIT 10;",
    tags: ["ORDER BY", "LIMIT"],
    hints: [
      "You want to sort descending first and then only see the first rows — combine sorting with limiting.",
      "Combine `ORDER BY column DESC` with `LIMIT n` at the end of the query.",
      "Sort `comments` descending by `created_at` and limit to 10 rows with `LIMIT 10`.",
    ],
    hiddenTestQuery: "SELECT * FROM comments ORDER BY created_at DESC LIMIT 10;",
    hiddenTestMode: "exact",
  })
);
