/**
 * Window Functions exercises (English).
 * Contains exercises for ROW_NUMBER, RANK, DENSE_RANK, LAG, LEAD, and PARTITION BY.
 */
import { makeWriteExercise, makeDebugExercise, makePredictExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";

export const windowFunctionExercisesEn: Exercise[] = [];
resetCounter();
windowFunctionExercisesEn.push(
  makeWriteExercise("win", {
    title: "ROW_NUMBER: Number products by price",
    description: "Show all products sorted by price from most to least expensive, with a sequential position number per row. The output contains product name, price, and the position number.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price, ROW_NUMBER() OVER (ORDER BY price DESC) AS rn FROM products;",
    tags: ["Window Functions", "ROW_NUMBER"],
    hints: [
      "Window Functions calculate a value per row without reducing the result set — unlike GROUP BY, all rows are preserved.",
      "Syntax: `ROW_NUMBER() OVER (ORDER BY column DESC) AS rn` — ROW_NUMBER always assigns unique, sequential numbers.",
      "Use ROW_NUMBER() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, price, ROW_NUMBER() OVER (ORDER BY price DESC) AS rn FROM products;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "ROW_NUMBER with Partition: Newest order per customer",
    description: "Show exactly one row per customer with their newest order — the most recent date per customer. The output contains customer ID, date, and total amount.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM (SELECT customer_id, date, total_amount, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY date DESC) AS rn FROM orders) sub WHERE rn = 1;",
    tags: ["Window Functions", "ROW_NUMBER", "PARTITION BY"],
    hints: [
      "PARTITION BY divides the data set into groups and restarts numbering in each group — so each customer gets their own row number sequence.",
      "Syntax: `ROW_NUMBER() OVER (PARTITION BY group_id ORDER BY date DESC) AS rn` — in the outer SELECT then `WHERE rn = 1` for only the first row per group.",
      "Use ROW_NUMBER() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT * FROM (SELECT customer_id, date, total_amount, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY date DESC) AS rn FROM orders) sub WHERE rn = 1;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "RANK: Employee ranking by salary",
    description: "Create a ranking of all employees by salary, where equal salaries receive the same rank and the next number is skipped. The output shows name, salary, and the rank.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS rank FROM employees;",
    tags: ["Window Functions", "RANK"],
    hints: [
      "RANK() is a window function that assigns the same rank to equal values and then skips numbers (e.g., 1, 1, 3) — creating a fair ranking.",
      "Syntax: `RANK() OVER (ORDER BY column DESC) AS rank` — without PARTITION BY, ranking is across all rows.",
      "Use RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS rank FROM employees;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "DENSE_RANK: Movies by rating",
    description: "Create a gap-free ranking of all movies by rating, where equal ratings receive the same rank and the next number follows directly — without skipped numbers. The output shows title, rating, and the rank.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, rating, DENSE_RANK() OVER (ORDER BY rating DESC) AS rank FROM movies;",
    tags: ["Window Functions", "DENSE_RANK"],
    hints: [
      "DENSE_RANK() assigns the same rank to ties like RANK() — but without gaps in the numbering (e.g., 1, 1, 2 instead of 1, 1, 3).",
      "Syntax: `DENSE_RANK() OVER (ORDER BY column DESC) AS rank` — the difference from RANK is only that no numbers are skipped.",
      "Use RANK() and DENSE_RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT title, rating, DENSE_RANK() OVER (ORDER BY rating DESC) AS rank FROM movies;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "RANK vs DENSE_RANK: Salary comparison",
    description: "Show each employee with name, salary, and two different rank values side by side: a rank where numbers are skipped after ties, and a gap-free rank. This makes the difference between both methods directly visible.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS rnk, DENSE_RANK() OVER (ORDER BY salary DESC) AS drnk FROM employees;",
    tags: ["Window Functions", "RANK", "DENSE_RANK"],
    hints: [
      "Multiple window functions can stand side by side in a SELECT — each has its own OVER() and calculates its value independently per row.",
      "Syntax: `RANK() OVER (ORDER BY column DESC) AS rnk, DENSE_RANK() OVER (ORDER BY column DESC) AS drnk` — both in the same SELECT list.",
      "Use RANK() and DENSE_RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS rnk, DENSE_RANK() OVER (ORDER BY salary DESC) AS drnk FROM employees;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "RANK partitioned: Salary rank per department",
    description: "Create a separate salary ranking per department, so the highest-paid employee in each department receives rank 1. The output shows name, salary, department ID, and the rank within the department.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary, department_id, RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rank FROM employees;",
    tags: ["Window Functions", "RANK", "PARTITION BY"],
    hints: [
      "PARTITION BY in a window function divides the data into groups — within each group, the ranking restarts at 1, independent of other groups.",
      "Syntax: `RANK() OVER (PARTITION BY group_id ORDER BY value DESC) AS rank` — each group (department) gets its own ranking.",
      "Use RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, department_id, RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rank FROM employees;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "LAG: Price comparison with previous product",
    description: "For each product, show its name and price as well as the price of the previous product by ID in an additional column. The first product has no predecessor and shows NULL there.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price, LAG(price) OVER (ORDER BY id) AS previous_price FROM products;",
    tags: ["Window Functions", "LAG"],
    hints: [
      "LAG() is a window function that accesses the value of the directly preceding row — without a self-join and without changing the result set.",
      "Syntax: `LAG(column) OVER (ORDER BY sort_column) AS previous_value` — the first row has no predecessor and returns NULL.",
      "Use LAG() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, price, LAG(price) OVER (ORDER BY id) AS previous_price FROM products;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "LAG: Weight difference from previous entry",
    description: "For each body data entry, show the current weight and how many kilograms it has changed compared to the previous measurement of the same user. Positive values mean weight gain, negative means loss — the first entry of a user shows NULL.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT user_id, date, weight_kg, weight_kg - LAG(weight_kg) OVER (PARTITION BY user_id ORDER BY date) AS diff FROM body_data;",
    tags: ["Window Functions", "LAG", "PARTITION BY"],
    hints: [
      "LAG() with PARTITION BY ensures that the predecessor value always comes from the same group (here: the same user) — so values from different users are not compared.",
      "Syntax: `LAG(column) OVER (PARTITION BY group_id ORDER BY date) AS previous_value` — then `current - previous_value AS diff` in the SELECT list.",
      "Use LAG() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT user_id, date, weight_kg, weight_kg - LAG(weight_kg) OVER (PARTITION BY user_id ORDER BY date) AS diff FROM body_data;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "LEAD: Show next transaction",
    description: "For each transaction on an account, show the amount and the amount of the chronologically next transaction of the same account as a preview. The last transaction of an account shows NULL.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT id, account_id, amount, date, LEAD(amount) OVER (PARTITION BY account_id ORDER BY date) AS next_amount FROM transactions;",
    tags: ["Window Functions", "LEAD", "PARTITION BY"],
    hints: [
      "LEAD() is the counterpart to LAG() — instead of the previous row, it accesses the next row, without a self-join and without changing the result set.",
      "Syntax: `LEAD(column) OVER (PARTITION BY group_id ORDER BY date) AS next_value` — the last row of a partition returns NULL.",
      "Use LEAD() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, account_id, amount, date, LEAD(amount) OVER (PARTITION BY account_id ORDER BY date) AS next_amount FROM transactions;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "SUM OVER: Running total of order amounts",
    description: "Show each order with its amount and the cumulative total of all orders up to and including this row, sorted chronologically. The last row thus shows the total revenue of all orders.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT id, date, total_amount, SUM(total_amount) OVER (ORDER BY date) AS running_total FROM orders;",
    tags: ["Window Functions", "SUM OVER", "Running Total"],
    hints: [
      "SUM() OVER() is a window function that calculates a running (cumulative) sum — each row receives the sum of all rows up to and including the current one.",
      "Syntax: `SUM(column) OVER (ORDER BY sort_column) AS running_total` — without PARTITION BY, the sum runs across the entire table.",
      "Use SUM() OVER with the appropriate PARTITION — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, date, total_amount, SUM(total_amount) OVER (ORDER BY date) AS running_total FROM orders;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "SUM OVER partitioned: Running total per account",
    description: "For each transaction, show the accumulated account balance up to that point — each account has its own running total that starts with its first transaction.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT id, account_id, amount, date, SUM(amount) OVER (PARTITION BY account_id ORDER BY date) AS running_balance FROM transactions;",
    tags: ["Window Functions", "SUM OVER", "PARTITION BY", "Running Total"],
    hints: [
      "SUM() OVER() with PARTITION BY restarts the running total for each group (here: each account) at 0 — creating an account-specific running balance.",
      "Syntax: `SUM(column) OVER (PARTITION BY group_id ORDER BY date) AS running_value` — PARTITION BY determines the group, ORDER BY the order of accumulation.",
      "Use SUM() OVER with the appropriate PARTITION — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, account_id, amount, date, SUM(amount) OVER (PARTITION BY account_id ORDER BY date) AS running_balance FROM transactions;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "AVG OVER: Average salary per department",
    description: "Show each employee with their individual salary and the average salary of their department in the same row, so the comparison is directly readable. All members of a department have the same average value in the additional column.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary, department_id, AVG(salary) OVER (PARTITION BY department_id) AS avg_salary FROM employees;",
    tags: ["Window Functions", "AVG OVER", "PARTITION BY"],
    hints: [
      "AVG() OVER() calculates an average per partition and displays it on each row — without reducing the result set as GROUP BY would.",
      "Syntax: `AVG(column) OVER (PARTITION BY group_id) AS avg_value` — all rows of the same group receive the same average value.",
      "Use AVG() OVER with the appropriate PARTITION — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, department_id, AVG(salary) OVER (PARTITION BY department_id) AS avg_salary FROM employees;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "COUNT OVER: Ticket count per agent",
    description: "Show all assigned tickets with title and priority, supplemented by the total number of tickets assigned to the respective agent. Each row of the same agent shows the same total count.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT title, priority, agent_id, COUNT(*) OVER (PARTITION BY agent_id) AS ticket_count FROM tickets WHERE agent_id IS NOT NULL;",
    tags: ["Window Functions", "COUNT OVER", "PARTITION BY"],
    hints: [
      "COUNT() OVER() counts rows per partition and displays the value on each row — each ticket thus receives the total number of all tickets of its agent as an additional column.",
      "Syntax: `COUNT(*) OVER (PARTITION BY group_id) AS count` — combined with `WHERE group_id IS NOT NULL` for only assigned records.",
      "Follow the syntax in the second hint and adapt it to this task.",
    ],
    hiddenTestQuery: "SELECT title, priority, agent_id, COUNT(*) OVER (PARTITION BY agent_id) AS ticket_count FROM tickets WHERE agent_id IS NOT NULL;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "DENSE_RANK partitioned: Workout rank per user",
    description: "Create a gap-free ranking of workouts per user by calories burned, so the most intense workout of each user receives rank 1. Equal calorie counts get the same rank without skipped numbers.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT id, user_id, calories_burned, DENSE_RANK() OVER (PARTITION BY user_id ORDER BY calories_burned DESC) AS rank FROM workouts;",
    tags: ["Window Functions", "DENSE_RANK", "PARTITION BY"],
    hints: [
      "DENSE_RANK() with PARTITION BY creates separate rankings per group — each user gets their own ranking of workouts by calories, without gaps in ties.",
      "Syntax: `DENSE_RANK() OVER (PARTITION BY group_id ORDER BY value DESC) AS rank` — the ranking restarts at 1 per partition.",
      "Use RANK() and DENSE_RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, user_id, calories_burned, DENSE_RANK() OVER (PARTITION BY user_id ORDER BY calories_burned DESC) AS rank FROM workouts;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "Top-N per group: The 3 most expensive products per category",
    description: "Show the three most expensive products of each category — a maximum of three entries per category, descending by price. The output contains product name, category ID, price, and the position within the category.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM (SELECT name, category_id, price, ROW_NUMBER() OVER (PARTITION BY category_id ORDER BY price DESC) AS rn FROM products) sub WHERE rn <= 3;",
    tags: ["Window Functions", "ROW_NUMBER", "Top-N", "PARTITION BY"],
    hints: [
      "The Top-N-per-group pattern uses ROW_NUMBER() with PARTITION BY to number rows within each group — an outer SELECT then filters to the first N numbers.",
      "Syntax: `SELECT * FROM (SELECT ..., ROW_NUMBER() OVER (PARTITION BY group_id ORDER BY value DESC) AS rn FROM ...) sub WHERE rn <= N`",
      "Use ROW_NUMBER() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT * FROM (SELECT name, category_id, price, ROW_NUMBER() OVER (PARTITION BY category_id ORDER BY price DESC) AS rn FROM products) sub WHERE rn <= 3;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "LAG with offset: Comparison with the value two rows back",
    description: "For each transaction, show the current amount and the amount of the transaction two rows before it on the same account. The first two transactions of an account have no such comparison value and show NULL.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT id, account_id, amount, date, LAG(amount, 2) OVER (PARTITION BY account_id ORDER BY date) AS amount_2_back FROM transactions;",
    tags: ["Window Functions", "LAG", "Offset"],
    hints: [
      "LAG() accepts an optional offset as the second parameter — LAG(column, 2) accesses not the direct predecessor row but two rows back.",
      "Syntax: `LAG(column, 2) OVER (PARTITION BY group_id ORDER BY sort_column) AS value_2_back` — the default offset is 1.",
      "Use LAG() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, account_id, amount, date, LAG(amount, 2) OVER (PARTITION BY account_id ORDER BY date) AS amount_2_back FROM transactions;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "AVG OVER: Comparison with the average",
    description: "Show each movie with its rating, the overall average of all movies, and the deviation of this movie from the average. Positive values mean better than average, negative means worse.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, rating, AVG(rating) OVER () AS avg_rating, rating - AVG(rating) OVER () AS diff FROM movies;",
    tags: ["Window Functions", "AVG OVER"],
    hints: [
      "AVG() OVER() with an empty OVER clause calculates the overall average across all rows — without PARTITION BY and without ORDER BY, no partitioning is performed.",
      "Syntax: `AVG(column) OVER () AS overall_avg` — the window function can also be used inline in calculations: `column - AVG(column) OVER () AS diff`.",
      "Use AVG() OVER with the appropriate PARTITION — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT title, rating, AVG(rating) OVER () AS avg_rating, rating - AVG(rating) OVER () AS diff FROM movies;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "Multiple window functions: Rank and average",
    description: "Show each employee with their global salary rank among all employees and the average salary of their department in a single output. This shows at a glance where someone stands in the overall company and within their own department.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary, department_id, RANK() OVER (ORDER BY salary DESC) AS salary_rank, AVG(salary) OVER (PARTITION BY department_id) AS avg_salary FROM employees;",
    tags: ["Window Functions", "RANK", "AVG OVER", "PARTITION BY"],
    hints: [
      "Multiple window functions in a SELECT can have different OVER clauses — e.g., a global rank without PARTITION BY and a department average with PARTITION BY.",
      "Syntax: `RANK() OVER (ORDER BY value DESC) AS rank, AVG(value) OVER (PARTITION BY group_id) AS avg_value` — both in the same SELECT list, different windows.",
      "Use RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, department_id, RANK() OVER (ORDER BY salary DESC) AS salary_rank, AVG(salary) OVER (PARTITION BY department_id) AS avg_salary FROM employees;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "ROW_NUMBER: Pagination – Page 2",
    description: "Show exactly the products at positions 11 to 20 of the overall list sorted descending by price — the second page of a paginated view. The output contains product name, price, and the global position number.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM (SELECT name, price, ROW_NUMBER() OVER (ORDER BY price DESC) AS rn FROM products) sub WHERE rn BETWEEN 11 AND 20;",
    tags: ["Window Functions", "ROW_NUMBER", "Pagination"],
    hints: [
      "ROW_NUMBER() in a subquery is an alternative to OFFSET-based pagination — it numbers all rows and the outer SELECT filters to the desired page range.",
      "Syntax: `SELECT * FROM (SELECT ..., ROW_NUMBER() OVER (ORDER BY column DESC) AS rn FROM ...) sub WHERE rn BETWEEN start AND end`",
      "Use ROW_NUMBER() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT * FROM (SELECT name, price, ROW_NUMBER() OVER (ORDER BY price DESC) AS rn FROM products) sub WHERE rn BETWEEN 11 AND 20;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("win", {
    title: "LAG and LEAD combined: Transaction analysis",
    description: "Show each transaction with the amount of the chronologically previous and next transaction of the same account as well as the difference from the predecessor. This shows at a glance which direction the amounts are moving.",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT id, account_id, amount, date, LAG(amount) OVER (PARTITION BY account_id ORDER BY date) AS prev_amount, LEAD(amount) OVER (PARTITION BY account_id ORDER BY date) AS next_amount, amount - LAG(amount) OVER (PARTITION BY account_id ORDER BY date) AS change FROM transactions;",
    tags: ["Window Functions", "LAG", "LEAD", "PARTITION BY"],
    hints: [
      "LAG() and LEAD() can stand together in one query — LAG fetches the predecessor, LEAD the successor, each partitioned per account and sorted by date.",
      "Syntax: `LAG(column) OVER (PARTITION BY group_id ORDER BY date) AS prev, LEAD(column) OVER (PARTITION BY group_id ORDER BY date) AS next` — same OVER clause for consistency.",
      "Use LAG() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT id, account_id, amount, date, LAG(amount) OVER (PARTITION BY account_id ORDER BY date) AS prev_amount, LEAD(amount) OVER (PARTITION BY account_id ORDER BY date) AS next_amount, amount - LAG(amount) OVER (PARTITION BY account_id ORDER BY date) AS change FROM transactions;",
    hiddenTestMode: "rows",
  }),
  makeDebugExercise("win", {
    title: "Debug: Missing PARTITION BY",
    description: "The query should output a separate salary ranking for each department, but the result shows a single global rank for all employees. Find and correct the error in the query.",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: hrDataset.id,
    brokenQuery: "SELECT name, salary, department_id, RANK() OVER (ORDER BY salary DESC) AS rank FROM employees;",
    referenceQuery: "SELECT name, salary, department_id, RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rank FROM employees;",
    tags: ["Window Functions", "RANK", "PARTITION BY", "Debugging"],
    hints: [
      "Without PARTITION BY, RANK() calculates a global rank across all employees — for a rank per department, PARTITION BY must be in the OVER clause.",
      "Syntax: `RANK() OVER (PARTITION BY group_id ORDER BY value DESC)` — PARTITION BY divides the data into groups, each group gets its own ranking.",
      "Use RANK() with the appropriate OVER clause — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: "SELECT name, salary, department_id, RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rank FROM employees;",
    hiddenTestMode: "rows",
  }),
  makePredictExercise("win", {
    title: "Prediction: RANK vs ROW_NUMBER",
    description: "What happens when multiple products have the same price?",
    difficulty: "intermediate",
    category: "Window Functions",
    datasetId: shopDataset.id,
    question: "If two products have the same price (e.g., 29.99) and ROW_NUMBER() or RANK() is used — what is the difference?",
    options: [
      { text: "ROW_NUMBER gives both 1, RANK gives 1 and 2", isCorrect: false },
      { text: "ROW_NUMBER gives 1 and 2, RANK gives both 1 (then 3 is assigned)", isCorrect: true },
      { text: "Both give 1 and 2, there is no difference", isCorrect: false },
      { text: "ROW_NUMBER gives 1 and 2, RANK gives 1 and 1 (then 2 is assigned)", isCorrect: false }
    ],
    tags: ["Window Functions", "RANK", "ROW_NUMBER", "Result Prediction"],
    hints: [
      "ROW_NUMBER() always assigns unique, sequential numbers — even with equal values, two rows get different numbers.",
      "RANK() assigns the same rank to rows with equal value and then skips numbers: with two rows at rank 1, rank 3 follows, not 2.",
      "Concrete example with price 29.99 twice: ROW_NUMBER yields 1 and 2; RANK yields 1 and 1 — then comes rank 3 (not 2), because rank 2 is skipped.",
    ],
  }),
  makePredictExercise("win", {
    title: "Prediction: Running total",
    description: "What does the running total show for orders?",
    difficulty: "advanced",
    category: "Window Functions",
    datasetId: shopDataset.id,
    question: "Given a table with orders (id=1, total_amount=100), (id=2, total_amount=200), (id=3, total_amount=50). What is the running total at the third order with `SUM(total_amount) OVER (ORDER BY id)`?",
    options: [
      { text: "50", isCorrect: false },
      { text: "350", isCorrect: true },
      { text: "100 + 200 + 50 = 350, but only in the last row", isCorrect: false },
      { text: "150", isCorrect: false }
    ],
    tags: ["Window Functions", "SUM OVER", "Result Prediction"],
    hints: [
      "SUM() OVER (ORDER BY id) calculates a running total — each row receives the sum of all rows from the first to the current (inclusive).",
      "Note: The running total grows from row to row: id=1 → 100, id=2 → 300, id=3 → 350.",
      "With SUM(total_amount) OVER (ORDER BY id): row id=1 yields 100, row id=2 yields 100+200=300, row id=3 yields 100+200+50=350.",
    ],
  })
);
