/**
 * Subquery exercises (English).
 * Contains exercises for nested SELECT queries (subqueries).
 */
import { makeWriteExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";

export const subqueryExercisesEn: Exercise[] = [];
resetCounter();
subqueryExercisesEn.push(
  makeWriteExercise("sub", {
    title: "Products with EXISTS subquery",
    description: "Show the name and price of all products for which there is at least one matching row in `order_items`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, price FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use `EXISTS` with a correlated subquery on `order_items` that checks `product_id = products.id`, and select `name, price` from `products`.",
    ],
    hiddenTestQuery: `SELECT name, price FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on products",
    description: "Show the name and price of all products that appear at least once in an order item.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, price FROM products WHERE id IN (SELECT product_id FROM order_items);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use `IN` with a subquery that selects `product_id` from `order_items`, and filter `products` on `id IN (...)`.",
    ],
    hiddenTestQuery: `SELECT name, price FROM products WHERE id IN (SELECT product_id FROM order_items);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: products (name and price)",
    description: "Output the name and price of all products by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT * FROM (SELECT name, price FROM products) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column1, column2 FROM table) AS alias` — the alias is mandatory in SQL.",
      "Write an inline view `(SELECT name, price FROM products) AS sub` and select everything from it with `SELECT *`.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name, price FROM products) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on products",
    description: "Show the name and price of each product together with the count of how often it appears in order items.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, price, (SELECT COUNT(*) FROM order_items WHERE order_items.product_id = products.id) AS count FROM products;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column1, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause: `(SELECT COUNT(*) FROM order_items WHERE order_items.product_id = products.id) AS count`.",
    ],
    hiddenTestQuery: `SELECT name, price, (SELECT COUNT(*) FROM order_items WHERE order_items.product_id = products.id) AS count FROM products;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Products with order items via EXISTS (variant)",
    description: "Show the name and price of all products for which there is at least one matching row in `order_items`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, price FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, price FROM products WHERE EXISTS (SELECT 1 FROM order_items WHERE order_items.product_id = products.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Customers with EXISTS subquery",
    description: "Show the name and email of all customers for whom there is at least one matching row in `orders`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, email FROM customers WHERE EXISTS (SELECT 1 FROM orders WHERE orders.customer_id = customers.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, email FROM customers WHERE EXISTS (SELECT 1 FROM orders WHERE orders.customer_id = customers.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on customers (Shop)",
    description: "Show the name and email of all customers who have placed at least one order.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, email FROM customers WHERE id IN (SELECT customer_id FROM orders);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, email FROM customers WHERE id IN (SELECT customer_id FROM orders);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: customers (Shop)",
    description: "Output the name and email of all customers by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT * FROM (SELECT name, email FROM customers) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column1, column2 FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name, email FROM customers) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on customers (Shop)",
    description: "Show the name and email of each customer together with the number of their orders so far.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, email, (SELECT COUNT(*) FROM orders WHERE orders.customer_id = customers.id) AS count FROM customers;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column1, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, email, (SELECT COUNT(*) FROM orders WHERE orders.customer_id = customers.id) AS count FROM customers;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Customers with orders via EXISTS (Shop)",
    description: "Show the name and email of all customers for whom there is at least one matching row in `orders`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "shop",
    referenceQuery: `SELECT name, email FROM customers WHERE EXISTS (SELECT 1 FROM orders WHERE orders.customer_id = customers.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, email FROM customers WHERE EXISTS (SELECT 1 FROM orders WHERE orders.customer_id = customers.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Users with EXISTS subquery",
    description: "Show the names of all users for whom there is at least one matching row in `workouts`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "fitness",
    referenceQuery: `SELECT name FROM users WHERE EXISTS (SELECT 1 FROM workouts WHERE workouts.user_id = users.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM users WHERE EXISTS (SELECT 1 FROM workouts WHERE workouts.user_id = users.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on users",
    description: "Show the names of all users who have completed at least one workout.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "fitness",
    referenceQuery: `SELECT name FROM users WHERE id IN (SELECT user_id FROM workouts);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM users WHERE id IN (SELECT user_id FROM workouts);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: users (name)",
    description: "Output the names of all users by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "fitness",
    referenceQuery: `SELECT * FROM (SELECT name FROM users) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name FROM users) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on users",
    description: "Show the name of each user together with the number of workouts they have completed.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "fitness",
    referenceQuery: `SELECT name, (SELECT COUNT(*) FROM workouts WHERE workouts.user_id = users.id) AS count FROM users;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, (SELECT COUNT(*) FROM workouts WHERE workouts.user_id = users.id) AS count FROM users;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Users with workouts via EXISTS (variant)",
    description: "Show the names of all users for whom there is at least one matching row in `workouts`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "fitness",
    referenceQuery: `SELECT name FROM users WHERE EXISTS (SELECT 1 FROM workouts WHERE workouts.user_id = users.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM users WHERE EXISTS (SELECT 1 FROM workouts WHERE workouts.user_id = users.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Departments with EXISTS subquery",
    description: "Show the name and location of all departments for which there is at least one matching row in `employees`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "hr",
    referenceQuery: `SELECT name, location FROM departments WHERE EXISTS (SELECT 1 FROM employees WHERE employees.department_id = departments.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, location FROM departments WHERE EXISTS (SELECT 1 FROM employees WHERE employees.department_id = departments.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on departments",
    description: "Show the name and location of all departments to which at least one employee is assigned.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "hr",
    referenceQuery: `SELECT name, location FROM departments WHERE id IN (SELECT department_id FROM employees);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, location FROM departments WHERE id IN (SELECT department_id FROM employees);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: departments (name and location)",
    description: "Output the name and location of all departments by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "hr",
    referenceQuery: `SELECT * FROM (SELECT name, location FROM departments) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column1, column2 FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name, location FROM departments) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on departments",
    description: "Show the name and location of each department together with the number of its employees.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "hr",
    referenceQuery: `SELECT name, location, (SELECT COUNT(*) FROM employees WHERE employees.department_id = departments.id) AS count FROM departments;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column1, column2, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, location, (SELECT COUNT(*) FROM employees WHERE employees.department_id = departments.id) AS count FROM departments;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Departments with employees via EXISTS (variant)",
    description: "Show the name and location of all departments for which there is at least one matching row in `employees`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "hr",
    referenceQuery: `SELECT name, location FROM departments WHERE EXISTS (SELECT 1 FROM employees WHERE employees.department_id = departments.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, location FROM departments WHERE EXISTS (SELECT 1 FROM employees WHERE employees.department_id = departments.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Agents with EXISTS subquery",
    description: "Show the name and team of all agents for whom there is at least one matching row in `tickets`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "tickets",
    referenceQuery: `SELECT name, team FROM agents WHERE EXISTS (SELECT 1 FROM tickets WHERE tickets.agent_id = agents.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, team FROM agents WHERE EXISTS (SELECT 1 FROM tickets WHERE tickets.agent_id = agents.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on agents",
    description: "Show the name and team of all agents who are assigned to at least one ticket.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "tickets",
    referenceQuery: `SELECT name, team FROM agents WHERE id IN (SELECT agent_id FROM tickets);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, team FROM agents WHERE id IN (SELECT agent_id FROM tickets);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: agents (name and team)",
    description: "Output the name and team of all agents by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "tickets",
    referenceQuery: `SELECT * FROM (SELECT name, team FROM agents) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column1, column2 FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name, team FROM agents) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on agents",
    description: "Show the name and team of each agent together with the total number of tickets assigned to them.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "tickets",
    referenceQuery: `SELECT name, team, (SELECT COUNT(*) FROM tickets WHERE tickets.agent_id = agents.id) AS count FROM agents;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column1, column2, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, team, (SELECT COUNT(*) FROM tickets WHERE tickets.agent_id = agents.id) AS count FROM agents;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Agents with tickets via EXISTS (variant)",
    description: "Show the name and team of all agents for whom there is at least one matching row in `tickets`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "tickets",
    referenceQuery: `SELECT name, team FROM agents WHERE EXISTS (SELECT 1 FROM tickets WHERE tickets.agent_id = agents.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, team FROM agents WHERE EXISTS (SELECT 1 FROM tickets WHERE tickets.agent_id = agents.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Customers with accounts via EXISTS",
    description: "Show the names of all customers for whom there is at least one matching row in `accounts`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "banking",
    referenceQuery: `SELECT name FROM customers WHERE EXISTS (SELECT 1 FROM accounts WHERE accounts.customer_id = customers.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM customers WHERE EXISTS (SELECT 1 FROM accounts WHERE accounts.customer_id = customers.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on customers (Banking)",
    description: "Show the names of all customers who own at least one account.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "banking",
    referenceQuery: `SELECT name FROM customers WHERE id IN (SELECT customer_id FROM accounts);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM customers WHERE id IN (SELECT customer_id FROM accounts);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: customers (Banking)",
    description: "Output the names of all customers by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "banking",
    referenceQuery: `SELECT * FROM (SELECT name FROM customers) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT name FROM customers) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on customers (Banking)",
    description: "Show the name of each customer together with the number of their accounts.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "banking",
    referenceQuery: `SELECT name, (SELECT COUNT(*) FROM accounts WHERE accounts.customer_id = customers.id) AS count FROM customers;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name, (SELECT COUNT(*) FROM accounts WHERE accounts.customer_id = customers.id) AS count FROM customers;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Customers with accounts via EXISTS (Banking)",
    description: "Show the names of all customers for whom there is at least one matching row in `accounts`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "banking",
    referenceQuery: `SELECT name FROM customers WHERE EXISTS (SELECT 1 FROM accounts WHERE accounts.customer_id = customers.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT name FROM customers WHERE EXISTS (SELECT 1 FROM accounts WHERE accounts.customer_id = customers.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Movies with EXISTS subquery",
    description: "Show the title and genre of all movies for which there is at least one matching row in `watch_history`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "streaming",
    referenceQuery: `SELECT title, genre FROM movies WHERE EXISTS (SELECT 1 FROM watch_history WHERE watch_history.movie_id = movies.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT title, genre FROM movies WHERE EXISTS (SELECT 1 FROM watch_history WHERE watch_history.movie_id = movies.id);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "IN subquery on movies",
    description: "Show the title and genre of all movies that have been watched at least once.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "streaming",
    referenceQuery: `SELECT title, genre FROM movies WHERE id IN (SELECT movie_id FROM watch_history);`,
    expectedResultText: "",
    tags: ["Subquery", "IN"],
    hints: [
      "IN with a subquery checks whether a value is contained in the result list of the inner query — useful when the subquery returns a list of IDs.",
      "Syntax: `WHERE column IN (SELECT foreign_id FROM other_table)` — the inner query may return multiple rows but only one column.",
      "Use IN with a subquery that selects the matching IDs — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT title, genre FROM movies WHERE id IN (SELECT movie_id FROM watch_history);`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Inline view: movies (title and genre)",
    description: "Output the title and genre of all movies by using an intermediate query as a data source.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "streaming",
    referenceQuery: `SELECT * FROM (SELECT title, genre FROM movies) AS sub;`,
    expectedResultText: "",
    tags: ["Subquery", "Inline view"],
    hints: [
      "A subquery in the FROM clause (inline view or derived table) creates a temporary table — it must always receive an alias so the outer SELECT can access it.",
      "Syntax: `FROM (SELECT column1, column2 FROM table) AS alias` — the alias is mandatory in SQL.",
      "Create an inline view as a subquery in the FROM clause with an alias — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT * FROM (SELECT title, genre FROM movies) AS sub;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Correlated subquery on movies",
    description: "Show the title and genre of each movie together with the number of times it has been watched.",
    difficulty: "advanced",
    category: "Subquery",
    datasetId: "streaming",
    referenceQuery: `SELECT title, genre, (SELECT COUNT(*) FROM watch_history WHERE watch_history.movie_id = movies.id) AS count FROM movies;`,
    expectedResultText: "",
    tags: ["Subquery", "Correlated"],
    hints: [
      "A correlated subquery in the SELECT clause is executed once for each row of the outer query and returns exactly one value — this allows per-row aggregations.",
      "Syntax: `SELECT column1, column2, (SELECT COUNT(*) FROM other_table WHERE other_table.foreign_id = main_table.id) AS alias FROM main_table`",
      "Use a correlated subquery in the SELECT clause with COUNT — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT title, genre, (SELECT COUNT(*) FROM watch_history WHERE watch_history.movie_id = movies.id) AS count FROM movies;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sub", {
    title: "Movies with watch history via EXISTS (variant)",
    description: "Show the title and genre of all movies for which there is at least one matching row in `watch_history`.",
    difficulty: "intermediate",
    category: "Subquery",
    datasetId: "streaming",
    referenceQuery: `SELECT title, genre FROM movies WHERE EXISTS (SELECT 1 FROM watch_history WHERE watch_history.movie_id = movies.id);`,
    expectedResultText: "",
    tags: ["Subquery", "EXISTS"],
    hints: [
      "EXISTS checks whether a correlated subquery returns at least one row — it is executed for each row of the outer query and returns true or false.",
      "Syntax: `WHERE EXISTS (SELECT 1 FROM other_table WHERE other_table.foreign_id = main_table.id)` — SELECT 1 is sufficient since only existence is checked.",
      "Use EXISTS with a correlated subquery — follow the syntax in the second hint.",
    ],
    hiddenTestQuery: `SELECT title, genre FROM movies WHERE EXISTS (SELECT 1 FROM watch_history WHERE watch_history.movie_id = movies.id);`,
    hiddenTestMode: "rows",
  })
);
