/**
 * WHERE clause exercises (English).
 * Contains exercises for filtering with WHERE conditions.
 */
import { makeWriteExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";

export const whereExercisesEn: Exercise[] = [];
resetCounter();
whereExercisesEn.push(
  makeWriteExercise("whr", {
    title: "Customers in Berlin",
    description: "Show all customers from the `customers` table who live in Berlin.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM customers WHERE city = 'Berlin';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You need a filter condition to return only rows with a specific city name.",
      "Use `WHERE column = 'value'` — text values must be enclosed in single quotes.",
      "The query: `SELECT * FROM customers WHERE city = 'Berlin';`",
    ],
    hiddenTestQuery: "SELECT * FROM customers WHERE city = 'Berlin';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Products more expensive than 50 euros",
    description: "Find the names and prices of all products with a price greater than 50.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products WHERE price > 50;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "You want to filter rows based on a numeric value — comparison operators are available for this.",
      "Use `WHERE column > value` — numbers don't need quotes.",
      "The query: `SELECT name, price FROM products WHERE price > 50;`",
    ],
    hiddenTestQuery: "SELECT name, price FROM products WHERE price > 50;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Orders with completed status",
    description: "Show all orders that have the status 'completed'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM orders WHERE status = 'completed';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see rows where a text column has a specific value.",
      "Use `WHERE column = 'value'` — text values belong in single quotes.",
      "The query: `SELECT * FROM orders WHERE status = 'completed';`",
    ],
    hiddenTestQuery: "SELECT * FROM orders WHERE status = 'completed';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Customers not from Munich",
    description: "Show all customers who do NOT live in Munich.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM customers WHERE city <> 'Munich';",
    tags: ["WHERE", "Not equal"],
    hints: [
      "You want to exclude all rows where a column has a specific value — there's a not-equal operator for this.",
      "Use `<>` or `!=` for 'not equal': `WHERE column <> 'value'`",
      "The query: `SELECT * FROM customers WHERE city <> 'Munich';`",
    ],
    hiddenTestQuery: "SELECT * FROM customers WHERE city <> 'Munich';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Fitness users over 80 kg",
    description: "Show the name and weight of all users who weigh more than 80 kg.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT name, weight_kg FROM users WHERE weight_kg > 80;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "You want to filter rows by a numeric threshold.",
      "Use `WHERE column > value` — numbers don't need quotes.",
      "The query: `SELECT name, weight_kg FROM users WHERE weight_kg > 80;`",
    ],
    hiddenTestQuery: "SELECT name, weight_kg FROM users WHERE weight_kg > 80;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Cardio exercises",
    description: "Find all exercises in the 'Cardio' category.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM exercises WHERE category = 'Cardio';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see exercises of a specific category — you need a filter condition on a text column.",
      "Use `WHERE column = 'value'` — text values must be in single quotes.",
      "The query: `SELECT * FROM exercises WHERE category = 'Cardio';`",
    ],
    hiddenTestQuery: "SELECT * FROM exercises WHERE category = 'Cardio';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Employees with salary over 50000",
    description: "Show the name and salary of all employees who earn more than 50,000 euros.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, salary FROM employees WHERE salary > 50000;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "You only want to see employees whose salary exceeds a certain value.",
      "Use a comparison operator: `WHERE column > value` — no quotes for numbers.",
      "The query: `SELECT name, salary FROM employees WHERE salary > 50000;`",
    ],
    hiddenTestQuery: "SELECT name, salary FROM employees WHERE salary > 50000;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Departments in Berlin",
    description: "Show all departments at the 'Berlin' location.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT * FROM departments WHERE location = 'Berlin';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see departments at a specific location — you need a filter condition.",
      "Use `WHERE column = 'value'` — text values in single quotes.",
      "The query: `SELECT * FROM departments WHERE location = 'Berlin';`",
    ],
    hiddenTestQuery: "SELECT * FROM departments WHERE location = 'Berlin';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Open tickets",
    description: "Find all tickets with the status 'open'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT * FROM tickets WHERE status = 'open';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to return only tickets with a specific status value.",
      "Use `WHERE column = 'value'` — text values must be in single quotes.",
      "The query: `SELECT * FROM tickets WHERE status = 'open';`",
    ],
    hiddenTestQuery: "SELECT * FROM tickets WHERE status = 'open';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Critical tickets",
    description: "Show the title and priority of all tickets with the priority 'critical'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT title, priority FROM tickets WHERE priority = 'critical';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see tickets of a specific urgency level — you need a filter condition on the priority column.",
      "Use `WHERE column = 'value'` — text values belong in single quotes.",
      "The query: `SELECT title, priority FROM tickets WHERE priority = 'critical';`",
    ],
    hiddenTestQuery: "SELECT title, priority FROM tickets WHERE priority = 'critical';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Find checking accounts",
    description: "Show all accounts of type 'Checking' from the banking dataset.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM accounts WHERE type = 'Checking';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to display only accounts of a specific type — you need a condition on the type column.",
      "Use `WHERE column = 'value'` — text values in single quotes.",
      "The query: `SELECT * FROM accounts WHERE type = 'Checking';`",
    ],
    hiddenTestQuery: "SELECT * FROM accounts WHERE type = 'Checking';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Outgoing transactions",
    description: "Find all transactions of type 'debit' (outflows).",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM transactions WHERE type = 'debit';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to filter out only transactions of a specific type.",
      "Use `WHERE column = 'value'` — text values must be in single quotes.",
      "The query: `SELECT * FROM transactions WHERE type = 'debit';`",
    ],
    hiddenTestQuery: "SELECT * FROM transactions WHERE type = 'debit';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Premium users",
    description: "Show all users with the 'Premium' subscription.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT * FROM users WHERE subscription = 'Premium';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to see only users with a specific subscription type.",
      "Use `WHERE column = 'value'` — text values belong in single quotes.",
      "The query: `SELECT * FROM users WHERE subscription = 'Premium';`",
    ],
    hiddenTestQuery: "SELECT * FROM users WHERE subscription = 'Premium';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Drama movies",
    description: "Find the title and year of all movies in the 'Drama' genre.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, year FROM movies WHERE genre = 'Drama';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to return only movies of a specific genre — you need a filter condition.",
      "Use `WHERE column = 'value'` — text values must be in single quotes.",
      "The query: `SELECT title, year FROM movies WHERE genre = 'Drama';`",
    ],
    hiddenTestQuery: "SELECT title, year FROM movies WHERE genre = 'Drama';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Chrome sessions",
    description: "Show all sessions that were conducted using the 'Chrome' browser.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT * FROM sessions WHERE browser = 'Chrome';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to filter sessions by the browser used.",
      "Use `WHERE column = 'value'` — text values in single quotes.",
      "The query: `SELECT * FROM sessions WHERE browser = 'Chrome';`",
    ],
    hiddenTestQuery: "SELECT * FROM sessions WHERE browser = 'Chrome';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Products between 20 and 100 euros",
    description: "Find the names and prices of all products whose price is between 20 and 100 (inclusive).",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products WHERE price BETWEEN 20 AND 100;",
    tags: ["WHERE", "BETWEEN"],
    hints: [
      "You want to filter rows whose value falls within a specific range — there's an operator exactly for this.",
      "`BETWEEN value1 AND value2` checks an inclusive range: `WHERE column BETWEEN 20 AND 100`",
      "The query: `SELECT name, price FROM products WHERE price BETWEEN 20 AND 100;`",
    ],
    hiddenTestQuery: "SELECT name, price FROM products WHERE price BETWEEN 20 AND 100;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Customers from Berlin or Hamburg",
    description: "Show all customers who live in Berlin or Hamburg.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM customers WHERE city IN ('Berlin', 'Hamburg');",
    tags: ["WHERE", "IN"],
    hints: [
      "You want to filter rows that have one of several possible values — there's a more elegant way than multiple ORs.",
      "`IN (value1, value2, ...)` checks whether a value appears in a list: `WHERE column IN ('a', 'b')`",
      "Use `IN` with the values `'Berlin'` and `'Hamburg'` on the `city` column in the `customers` table.",
    ],
    hiddenTestQuery: "SELECT * FROM customers WHERE city IN ('Berlin', 'Hamburg');",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Products in categories 1 or 3",
    description: "Show the name and category_id of all products with category_id 1 or 3 (Electronics or Clothing).",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, category_id FROM products WHERE category_id IN (1, 3);",
    tags: ["WHERE", "IN"],
    hints: [
      "You want to query products from multiple categories at once — there's a compact operator for this.",
      "`IN (value1, value2)` checks against a list of values: `WHERE column IN (1, 3)` — numbers without quotes.",
      "The query: `SELECT name, category_id FROM products WHERE category_id IN (1, 3);`",
    ],
    hiddenTestQuery: "SELECT name, category_id FROM products WHERE category_id IN (1, 3);",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Customer name starts with M",
    description: "Find the names of all customers whose name begins with 'M'.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name FROM customers WHERE name LIKE 'M%';",
    tags: ["WHERE", "LIKE"],
    hints: [
      "You want to filter text values by a pattern — for example, by the first letter.",
      "`LIKE` enables pattern matching: `WHERE column LIKE 'M%'` — the `%` stands for any number of characters.",
      "The query: `SELECT name FROM customers WHERE name LIKE 'M%';`",
    ],
    hiddenTestQuery: "SELECT name FROM customers WHERE name LIKE 'M%';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Email contains example.com",
    description: "Find the name and email address of all customers whose email contains 'example.com'.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, email FROM customers WHERE email LIKE '%example.com';",
    tags: ["WHERE", "LIKE"],
    hints: [
      "You want to filter email addresses by their ending — this requires pattern recognition in SQL.",
      "`LIKE` with `%` as a wildcard: `WHERE column LIKE '%pattern'` — `%` stands for any characters before it.",
      "The query: `SELECT name, email FROM customers WHERE email LIKE '%example.com';`",
    ],
    hiddenTestQuery: "SELECT name, email FROM customers WHERE email LIKE '%example.com';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Workouts longer than 60 minutes",
    description: "Show all workouts that last longer than 60 minutes.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM workouts WHERE duration_min > 60;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "You want to filter rows by a numeric minimum value.",
      "Use a comparison operator: `WHERE column > value` — no quotes for numbers.",
      "The query: `SELECT * FROM workouts WHERE duration_min > 60;`",
    ],
    hiddenTestQuery: "SELECT * FROM workouts WHERE duration_min > 60;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Chest exercises",
    description: "Find all exercises for the 'Chest' muscle group.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM exercises WHERE muscle_group = 'Chest';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see exercises for a specific muscle group — you need a filter condition.",
      "Use `WHERE column = 'value'` — text values in single quotes.",
      "The query: `SELECT * FROM exercises WHERE muscle_group = 'Chest';`",
    ],
    hiddenTestQuery: "SELECT * FROM exercises WHERE muscle_group = 'Chest';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Employees without a manager",
    description: "Find the name and position of all employees who have no manager (manager_id is NULL).",
    difficulty: "junior",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, position FROM employees WHERE manager_id IS NULL;",
    tags: ["WHERE", "IS NULL"],
    hints: [
      "When a column has no value, it's called NULL — a special state meaning 'no value'.",
      "Use `IS NULL` to check for NULL — never use `= NULL`, that doesn't work in SQL.",
      "The condition: `SELECT name, position FROM employees WHERE manager_id IS NULL;`",
    ],
    hiddenTestQuery: "SELECT name, position FROM employees WHERE manager_id IS NULL;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Approved vacation",
    description: "Show all vacation requests that have been approved.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT * FROM vacation WHERE approved = 1;",
    tags: ["WHERE", "Boolean value"],
    hints: [
      "You only want to see approved entries — in SQL, boolean values are often stored as 1 (true) and 0 (false).",
      "Compare a boolean column with a number: `WHERE column = 1` for true, `WHERE column = 0` for false.",
      "The query: `SELECT * FROM vacation WHERE approved = 1;`",
    ],
    hiddenTestQuery: "SELECT * FROM vacation WHERE approved = 1;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Inactive agents",
    description: "Find all agents who are not active.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT * FROM agents WHERE active = 0;",
    tags: ["WHERE", "Boolean value"],
    hints: [
      "You only want to see inactive entries — boolean values in SQL are often stored as 1 or 0.",
      "Use `WHERE column = 0` for 'false' or inactive: no text, just the number 0.",
      "The query: `SELECT * FROM agents WHERE active = 0;`",
    ],
    hiddenTestQuery: "SELECT * FROM agents WHERE active = 0;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Tickets without an assigned agent",
    description: "Find all tickets that have no agent assigned.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT * FROM tickets WHERE agent_id IS NULL;",
    tags: ["WHERE", "IS NULL"],
    hints: [
      "When no agent is assigned, the value is completely missing — in SQL this is called NULL.",
      "Use `IS NULL` to find missing values: `WHERE column IS NULL` — `= NULL` doesn't work in SQL.",
      "The query: `SELECT * FROM tickets WHERE agent_id IS NULL;`",
    ],
    hiddenTestQuery: "SELECT * FROM tickets WHERE agent_id IS NULL;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Savings accounts with balance over 10000",
    description: "Find all savings accounts with a balance greater than 10,000.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM accounts WHERE type = 'Savings' AND balance > 10000;",
    tags: ["WHERE", "AND"],
    hints: [
      "You want to filter by account type and balance simultaneously — both conditions must be true.",
      "Combine multiple conditions with `AND`: `WHERE condition1 AND condition2`",
      "The query: `SELECT * FROM accounts WHERE type = 'Savings' AND balance > 10000;`",
    ],
    hiddenTestQuery: "SELECT * FROM accounts WHERE type = 'Savings' AND balance > 10000;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Transactions with high amounts",
    description: "Find all transactions with an amount less than -1000 (large outflows).",
    difficulty: "junior",
    category: "WHERE",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT * FROM transactions WHERE amount < -1000;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "Outflows are stored as negative numbers — you want to filter the particularly large outflows.",
      "Use `WHERE column < value` — negative numbers are written without quotes.",
      "The query: `SELECT * FROM transactions WHERE amount < -1000;`",
    ],
    hiddenTestQuery: "SELECT * FROM transactions WHERE amount < -1000;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Movies after 2010",
    description: "Show the title and year of all movies released after 2010.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, year FROM movies WHERE year > 2010;",
    tags: ["WHERE", "Comparison operator"],
    hints: [
      "You want to filter movies by their release year — you need a comparison operator for this.",
      "Use `WHERE column > value` for 'greater than': `WHERE year > 2010`",
      "The query: `SELECT title, year FROM movies WHERE year > 2010;`",
    ],
    hiddenTestQuery: "SELECT title, year FROM movies WHERE year > 2010;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Sessions without a user",
    description: "Find all sessions from anonymous users (user_id is NULL).",
    difficulty: "junior",
    category: "WHERE",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT * FROM sessions WHERE user_id IS NULL;",
    tags: ["WHERE", "IS NULL"],
    hints: [
      "Anonymous users have no ID — the missing value is represented as NULL in SQL.",
      "Use `IS NULL` to find missing values: `WHERE column IS NULL` — `= NULL` is invalid in SQL.",
      "The query: `SELECT * FROM sessions WHERE user_id IS NULL;`",
    ],
    hiddenTestQuery: "SELECT * FROM sessions WHERE user_id IS NULL;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Critical errors",
    description: "Show all errors with the severity 'critical'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT * FROM errors WHERE severity = 'critical';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see errors of a specific severity level — you need a filter condition.",
      "Use `WHERE column = 'value'` — text values in single quotes.",
      "The query: `SELECT * FROM errors WHERE severity = 'critical';`",
    ],
    hiddenTestQuery: "SELECT * FROM errors WHERE severity = 'critical';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Page view events",
    description: "Find all events of type 'pageview'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT * FROM events WHERE event_type = 'pageview';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You want to filter out only events of a specific type.",
      "Use `WHERE column = 'value'` — text values belong in single quotes.",
      "The query: `SELECT * FROM events WHERE event_type = 'pageview';`",
    ],
    hiddenTestQuery: "SELECT * FROM events WHERE event_type = 'pageview';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Products under 30 euros or over 500 euros",
    description: "Find the name and price of all products that cost either under 30 euros or over 500 euros.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products WHERE price < 30 OR price > 500;",
    tags: ["WHERE", "OR"],
    hints: [
      "You want to return rows that satisfy one of two conditions — at least one must be true.",
      "Combine two conditions with `OR`: `WHERE condition1 OR condition2` — it's enough if one applies.",
      "The query: `SELECT name, price FROM products WHERE price < 30 OR price > 500;`",
    ],
    hiddenTestQuery: "SELECT name, price FROM products WHERE price < 30 OR price > 500;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Employees in department 1 or 2",
    description: "Show the name and department_id of all employees who work in department 1 or 2.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, department_id FROM employees WHERE department_id IN (1, 2);",
    tags: ["WHERE", "IN"],
    hints: [
      "You want to query employees from multiple departments at once — there's a compact operator for this.",
      "`IN (value1, value2)` checks against a list of values: `WHERE column IN (1, 2)` — more elegant than multiple ORs.",
      "The query: `SELECT name, department_id FROM employees WHERE department_id IN (1, 2);`",
    ],
    hiddenTestQuery: "SELECT name, department_id FROM employees WHERE department_id IN (1, 2);",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Applications in interview stage",
    description: "Find all applications with the status 'interview'.",
    difficulty: "beginner",
    category: "WHERE",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT * FROM applications WHERE status = 'interview';",
    tags: ["WHERE", "Equality"],
    hints: [
      "You only want to see applications at a specific stage of the process.",
      "Use `WHERE column = 'value'` — text values must be in single quotes.",
      "The query: `SELECT * FROM applications WHERE status = 'interview';`",
    ],
    hiddenTestQuery: "SELECT * FROM applications WHERE status = 'interview';",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Shipped or cancelled orders",
    description: "Show all orders that are either 'shipped' or 'cancelled'.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM orders WHERE status IN ('shipped', 'cancelled');",
    tags: ["WHERE", "IN"],
    hints: [
      "You want to find orders with one of two possible status values — there's an elegant operator for this.",
      "`IN ('value1', 'value2')` checks multiple text values at once: `WHERE column IN ('a', 'b')`",
      "The query: `SELECT * FROM orders WHERE status IN ('shipped', 'cancelled');`",
    ],
    hiddenTestQuery: "SELECT * FROM orders WHERE status IN ('shipped', 'cancelled');",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("whr", {
    title: "Movies between 120 and 150 minutes",
    description: "Find the title and duration of all movies that last between 120 and 150 minutes.",
    difficulty: "junior",
    category: "WHERE",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT title, duration_min FROM movies WHERE duration_min BETWEEN 120 AND 150;",
    tags: ["WHERE", "BETWEEN"],
    hints: [
      "You want to filter movies by a duration range — there's a clean operator for this.",
      "`BETWEEN value1 AND value2` checks an inclusive range: `WHERE column BETWEEN 120 AND 150`",
      "The query: `SELECT title, duration_min FROM movies WHERE duration_min BETWEEN 120 AND 150;`",
    ],
    hiddenTestQuery: "SELECT title, duration_min FROM movies WHERE duration_min BETWEEN 120 AND 150;",
    hiddenTestMode: "rows",
  })
);
