/**
 * Aggregation exercises (English).
 * Contains exercises for GROUP BY, SUM, COUNT, AVG, MIN, MAX, and HAVING.
 */
import { makeWriteExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";

export const aggregationExercisesEn: Exercise[] = [];
resetCounter();
aggregationExercisesEn.push(
  makeWriteExercise("agg", {
    title: "Number of rows in customers",
    description: "How many customers are stored in the database in total? Name the result column `count`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "shop",
    referenceQuery: `SELECT COUNT(*) AS count FROM customers;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to know how many records exist in a table — there's a special counting function for this.",
      "`COUNT(*)` counts all rows: `SELECT COUNT(*) AS alias FROM table`",
      "Combine `COUNT(*)` with the `customers` table and don't forget the alias `count`.",
    ],
    hiddenTestQuery: `SELECT COUNT(*) AS count FROM customers;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Sum of weight_kg",
    description: "What is the combined weight of all users? Name the result column `total`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "fitness",
    referenceQuery: `SELECT SUM(weight_kg) AS total FROM users;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to add up all numeric values in a column — an aggregate function does this for you.",
      "`SUM(column)` calculates the sum of all values: `SELECT SUM(columnname) AS alias FROM table`",
      "Use `SUM` on the `weight_kg` column in the `users` table with the alias `total`.",
    ],
    hiddenTestQuery: `SELECT SUM(weight_kg) AS total FROM users;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Average repetitions",
    description: "How many repetitions are performed on average per set? Name the result column `average`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "fitness",
    referenceQuery: `SELECT AVG(repetitions) AS average FROM sets;`,
    expectedResultText: "",
    tags: ["Aggregation", "AVG"],
    hints: [
      "You want to calculate the mean of all values in a column — there's an aggregate function for this.",
      "`AVG(column)` calculates the average: `SELECT AVG(columnname) AS alias FROM table`",
      "Use `AVG` on the `repetitions` column in the `sets` table with the alias `average`.",
    ],
    hiddenTestQuery: `SELECT AVG(repetitions) AS average FROM sets;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Highest department budget",
    description: "What is the largest budget among all departments? Name the result column `maximum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "hr",
    referenceQuery: `SELECT MAX(budget) AS maximum FROM departments;`,
    expectedResultText: "",
    tags: ["Aggregation", "MAX"],
    hints: [
      "You want to find the largest value in a column — an aggregate function gives you exactly that.",
      "`MAX(column)` returns the highest value: `SELECT MAX(columnname) AS alias FROM table`",
      "Use `MAX` on the `budget` column in the `departments` table with the alias `maximum`.",
    ],
    hiddenTestQuery: `SELECT MAX(budget) AS maximum FROM departments;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Minimum salary",
    description: "What is the lowest salary among all employees? Name the result column `minimum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "hr",
    referenceQuery: `SELECT MIN(salary) AS minimum FROM employees;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to find the smallest value in a column — an aggregate function gives you exactly that.",
      "`MIN(column)` returns the lowest value: `SELECT MIN(columnname) AS alias FROM table`",
      "Use `MIN` on the `salary` column in the `employees` table with the alias `minimum`.",
    ],
    hiddenTestQuery: `SELECT MIN(salary) AS minimum FROM employees;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Group by team",
    description: "For each team of agents, show how many agents belong to it. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: "tickets",
    referenceQuery: `SELECT team, COUNT(*) AS count FROM agents GROUP BY team;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Use `GROUP BY team` on the `agents` table and count with `COUNT(*)`.",
    ],
    hiddenTestQuery: `SELECT team, COUNT(*) AS count FROM agents GROUP BY team;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "HAVING in customers",
    description: "Show all addresses where more than one customer is registered, along with the respective count. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: "banking",
    referenceQuery: `SELECT address, COUNT(*) AS count FROM customers GROUP BY address HAVING COUNT(*) > 1;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to filter groups by their aggregation — similar to WHERE, but AFTER grouping.",
      "`HAVING` filters groups: `GROUP BY column HAVING COUNT(*) > threshold`",
      "Group `customers` by `address`, count with `COUNT(*)` and filter with `HAVING COUNT(*) > 1`.",
    ],
    hiddenTestQuery: `SELECT address, COUNT(*) AS count FROM customers GROUP BY address HAVING COUNT(*) > 1;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of rows in accounts",
    description: "How many accounts are there in total in the database? Name the result column `count`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "banking",
    referenceQuery: `SELECT COUNT(*) AS count FROM accounts;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to know how many records exist in a table — there's a special counting function for this.",
      "`COUNT(*)` counts all rows: `SELECT COUNT(*) AS alias FROM table`",
      "Combine `COUNT(*)` with the `accounts` table and don't forget the alias `count`.",
    ],
    hiddenTestQuery: `SELECT COUNT(*) AS count FROM accounts;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Total stock",
    description: "How many products are in stock in total? Name the result column `total`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "shop",
    referenceQuery: `SELECT SUM(stock) AS total FROM products;`,
    expectedResultText: "",
    tags: ["Aggregation", "SUM"],
    hints: [
      "You want to add up all numeric values in a column — an aggregate function does this for you.",
      "`SUM(column)` calculates the sum of all values: `SELECT SUM(columnname) AS alias FROM table`",
      "Use `SUM` on the `stock` column in the `products` table with the alias `total`.",
    ],
    hiddenTestQuery: `SELECT SUM(stock) AS total FROM products;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Average movie duration",
    description: "How long are the movies on average in minutes? Name the result column `average`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "streaming",
    referenceQuery: `SELECT AVG(duration_min) AS average FROM movies;`,
    expectedResultText: "",
    tags: ["Aggregation", "AVG"],
    hints: [
      "You want to calculate the mean of all values in a column — there's an aggregate function for this.",
      "`AVG(column)` calculates the average: `SELECT AVG(columnname) AS alias FROM table`",
      "Use `AVG` on the `duration_min` column in the `movies` table with the alias `average`.",
    ],
    hiddenTestQuery: `SELECT AVG(duration_min) AS average FROM movies;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Maximum duration_ms",
    description: "How long was the longest event in milliseconds? Name the result column `maximum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "logs",
    referenceQuery: `SELECT MAX(duration_ms) AS maximum FROM events;`,
    expectedResultText: "",
    tags: ["Aggregation", "GROUP BY", "HAVING"],
    hints: [
      "You want to find the largest value in a column — an aggregate function gives you exactly that.",
      "`MAX(column)` returns the highest value: `SELECT MAX(columnname) AS alias FROM table`",
      "Use `MAX` on the `duration_ms` column in the `events` table with the alias `maximum`.",
    ],
    hiddenTestQuery: `SELECT MAX(duration_ms) AS maximum FROM events;`,
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Smallest order value",
    description: "What is the lowest total amount among all orders? Name the result column `minimum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: "shop",
    referenceQuery: `SELECT MIN(total_amount) AS minimum FROM orders;`,
    expectedResultText: "",
    tags: ["Aggregation", "MIN"],
    hints: [
      "You want to find the smallest value in a column — an aggregate function gives you exactly that.",
      "`MIN(column)` returns the lowest value: `SELECT MIN(columnname) AS alias FROM table`",
      "Use `MIN` on the `total_amount` column in the `orders` table with the alias `minimum`.",
    ],
    hiddenTestQuery: `SELECT MIN(total_amount) AS minimum FROM orders;`,
    hiddenTestMode: "rows",
  })
);

aggregationExercisesEn.push(
  makeWriteExercise("agg", {
    title: "Average price of products",
    description: "What does a product cost on average? Name the result column `average_price`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT AVG(price) AS average_price FROM products;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "You want to calculate the mean of all values in a column — there's an aggregate function for this.",
      "`AVG(column)` calculates the average: `SELECT AVG(columnname) AS alias FROM table`",
      "Use `AVG` on the `price` column in the `products` table with the alias `average_price`.",
    ],
    hiddenTestQuery: "SELECT AVG(price) AS average_price FROM products;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Most expensive product",
    description: "What is the highest price a product has in the database? Name the result column `highest_price`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT MAX(price) AS highest_price FROM products;",
    tags: ["Aggregation", "MAX"],
    hints: [
      "You want to find the largest value in a column — an aggregate function gives you exactly that.",
      "`MAX(column)` returns the highest value: `SELECT MAX(columnname) AS alias FROM table`",
      "Use `MAX` on the `price` column in the `products` table with the alias `highest_price`.",
    ],
    hiddenTestQuery: "SELECT MAX(price) AS highest_price FROM products;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Sum of all order totals",
    description: "What is the total revenue from all orders combined? Name the result column `sum`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT SUM(total_amount) AS sum FROM orders;",
    tags: ["Aggregation", "SUM"],
    hints: [
      "You want to add up all numeric values in a column — an aggregate function does this for you.",
      "`SUM(column)` calculates the sum of all values: `SELECT SUM(columnname) AS alias FROM table`",
      "Use `SUM` on the `total_amount` column in the `orders` table with the alias `sum`.",
    ],
    hiddenTestQuery: "SELECT SUM(total_amount) AS sum FROM orders;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of products per category",
    description: "For each category, show how many products are assigned to it. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT category_id, COUNT(*) AS count FROM products GROUP BY category_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and aggregate per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `products` by `category_id` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT category_id, COUNT(*) AS count FROM products GROUP BY category_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Orders per customer",
    description: "For each customer, show how many orders they have placed. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT customer_id, COUNT(*) AS count FROM orders GROUP BY customer_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `orders` by `customer_id` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT customer_id, COUNT(*) AS count FROM orders GROUP BY customer_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Highest salary per department",
    description: "For each department, show the highest salary paid there. Name the aggregate column `max_salary`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT department_id, MAX(salary) AS max_salary FROM employees GROUP BY department_id;",
    tags: ["Aggregation", "GROUP BY", "MAX"],
    hints: [
      "You want to find the largest value of a column per group — combine grouping with an aggregate function.",
      "`GROUP BY column` groups, `MAX(column)` gives the highest value per group: `SELECT groupcolumn, MAX(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `employees` by `department_id` and use `MAX(salary)` as `max_salary`.",
    ],
    hiddenTestQuery: "SELECT department_id, MAX(salary) AS max_salary FROM employees GROUP BY department_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Average weight of users",
    description: "Calculate the average weight of all users. Name the result column `avg_weight`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT AVG(weight_kg) AS avg_weight FROM users;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "You want to calculate the mean of all values in a column — there's an aggregate function for this.",
      "`AVG(column)` calculates the average: `SELECT AVG(columnname) AS alias FROM table`",
      "Use `AVG` on the `weight_kg` column in the `users` table with the alias `avg_weight`.",
    ],
    hiddenTestQuery: "SELECT AVG(weight_kg) AS avg_weight FROM users;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Average calories per workout",
    description: "How many calories are burned on average during a workout? Name the result column `avg_calories`.",
    difficulty: "beginner",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT AVG(calories_burned) AS avg_calories FROM workouts;",
    tags: ["Aggregation", "AVG"],
    hints: [
      "You want to calculate the mean of all values in a column — there's an aggregate function for this.",
      "`AVG(column)` calculates the average: `SELECT AVG(columnname) AS alias FROM table`",
      "Use `AVG` on the `calories_burned` column in the `workouts` table with the alias `avg_calories`.",
    ],
    hiddenTestQuery: "SELECT AVG(calories_burned) AS avg_calories FROM workouts;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Sum calories per user",
    description: "For each user, show how many calories they have burned in total across all workouts. Name the aggregate column `total_calories`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT user_id, SUM(calories_burned) AS total_calories FROM workouts GROUP BY user_id;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "You want to calculate the sum of a column per group — combine grouping with a sum function.",
      "`GROUP BY column` groups, `SUM(column)` sums per group: `SELECT groupcolumn, SUM(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `workouts` by `user_id` and sum `calories_burned` as `total_calories`.",
    ],
    hiddenTestQuery: "SELECT user_id, SUM(calories_burned) AS total_calories FROM workouts GROUP BY user_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of tickets per priority",
    description: "For each priority level, show how many tickets are assigned to it. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT priority, COUNT(*) AS count FROM tickets GROUP BY priority;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `tickets` by `priority` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT priority, COUNT(*) AS count FROM tickets GROUP BY priority;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Total sum of transactions per account",
    description: "For each account, show the total amount of all associated transactions. Name the aggregate column `total`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT account_id, SUM(amount) AS total FROM transactions GROUP BY account_id;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "You want to calculate the sum of a column per group — combine grouping with a sum function.",
      "`GROUP BY column` groups, `SUM(column)` sums per group: `SELECT groupcolumn, SUM(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `transactions` by `account_id` and sum `amount` as `total`.",
    ],
    hiddenTestQuery: "SELECT account_id, SUM(amount) AS total FROM transactions GROUP BY account_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of movies per genre",
    description: "For each genre, show how many movies exist in the database. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT genre, COUNT(*) AS count FROM movies GROUP BY genre;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `movies` by `genre` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT genre, COUNT(*) AS count FROM movies GROUP BY genre;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Average rating per movie",
    description: "For each movie, show how many stars it received on average. Name the aggregate column `avg_stars`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT movie_id, AVG(stars) AS avg_stars FROM reviews GROUP BY movie_id;",
    tags: ["Aggregation", "GROUP BY", "AVG"],
    hints: [
      "You want to calculate the average of a column per group — combine grouping with an average function.",
      "`GROUP BY column` groups, `AVG(column)` calculates the mean per group: `SELECT groupcolumn, AVG(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `reviews` by `movie_id` and use `AVG(stars)` as `avg_stars`.",
    ],
    hiddenTestQuery: "SELECT movie_id, AVG(stars) AS avg_stars FROM reviews GROUP BY movie_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Count events per type",
    description: "For each event type, show how many events of that kind were recorded. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT event_type, COUNT(*) AS count FROM events GROUP BY event_type;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `events` by `event_type` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT event_type, COUNT(*) AS count FROM events GROUP BY event_type;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Customers with more than one order (HAVING)",
    description: "For each customer, show the number of their orders, but only if they have placed more than one. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT customer_id, COUNT(*) AS count FROM orders GROUP BY customer_id HAVING COUNT(*) > 1;",
    tags: ["Aggregation", "HAVING"],
    hints: [
      "You want to filter groups by their aggregation — similar to WHERE, but AFTER grouping.",
      "`HAVING` filters groups: `GROUP BY column HAVING COUNT(*) > threshold`",
      "Group `orders` by `customer_id`, count with `COUNT(*)` as `count` and filter with `HAVING COUNT(*) > 1`.",
    ],
    hiddenTestQuery: "SELECT customer_id, COUNT(*) AS count FROM orders GROUP BY customer_id HAVING COUNT(*) > 1;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Products more expensive than average",
    description: "Show the name and price of all products that are more expensive than the average of all products.",
    difficulty: "intermediate",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, price FROM products WHERE price > (SELECT AVG(price) FROM products);",
    tags: ["Aggregation", "Subquery", "AVG"],
    hints: [
      "You want to filter rows whose value is above a calculated average — you can use a subquery in the WHERE clause for this.",
      "A subquery in WHERE calculates the comparison value: `WHERE column > (SELECT AVG(column) FROM table)`",
      "Use a subquery: `WHERE price > (SELECT ...)` with `AVG(price)` from the same table.",
    ],
    hiddenTestQuery: "SELECT name, price FROM products WHERE price > (SELECT AVG(price) FROM products);",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Employees with more than 2 vacation days",
    description: "For each employee, show their total vacation days, but only if it's more than 2. Name the aggregate column `vacation_days`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT employee_id, SUM(days) AS vacation_days FROM vacation GROUP BY employee_id HAVING SUM(days) > 2;",
    tags: ["Aggregation", "HAVING", "SUM"],
    hints: [
      "You want to filter groups by their summed aggregation — similar to WHERE, but AFTER grouping.",
      "`HAVING` filters groups by aggregate values: `GROUP BY column HAVING SUM(valuecolumn) > threshold`",
      "Group `vacation` by `employee_id`, sum `days` as `vacation_days` and filter with `HAVING SUM(days) > 2`.",
    ],
    hiddenTestQuery: "SELECT employee_id, SUM(days) AS vacation_days FROM vacation GROUP BY employee_id HAVING SUM(days) > 2;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Minimum salary per department",
    description: "For each department, show the lowest salary paid there. Name the aggregate column `min_salary`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT department_id, MIN(salary) AS min_salary FROM employees GROUP BY department_id;",
    tags: ["Aggregation", "GROUP BY", "MIN"],
    hints: [
      "You want to find the smallest value of a column per group — combine grouping with an aggregate function.",
      "`GROUP BY column` groups, `MIN(column)` gives the minimum value per group: `SELECT groupcolumn, MIN(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `employees` by `department_id` and use `MIN(salary)` as `min_salary`.",
    ],
    hiddenTestQuery: "SELECT department_id, MIN(salary) AS min_salary FROM employees GROUP BY department_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of workouts per user",
    description: "For each user, show how many workouts they have completed. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT user_id, COUNT(*) AS count FROM workouts GROUP BY user_id;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `workouts` by `user_id` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT user_id, COUNT(*) AS count FROM workouts GROUP BY user_id;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of errors per severity",
    description: "For each severity level, show how many errors of that category occurred. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT severity, COUNT(*) AS count FROM errors GROUP BY severity;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `errors` by `severity` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT severity, COUNT(*) AS count FROM errors GROUP BY severity;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Number of accounts per type",
    description: "For each account type, show how many accounts of that type exist. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT type, COUNT(*) AS count FROM accounts GROUP BY type;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `accounts` by `type` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT type, COUNT(*) AS count FROM accounts GROUP BY type;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Total revenue per payment method",
    description: "For each payment method, show the total amount of all payments made with it. Name the aggregate column `total`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT payment_method, SUM(amount) AS total FROM payments GROUP BY payment_method;",
    tags: ["Aggregation", "GROUP BY", "SUM"],
    hints: [
      "You want to calculate the sum of a column per group — combine grouping with a sum function.",
      "`GROUP BY column` groups, `SUM(column)` sums per group: `SELECT groupcolumn, SUM(valuecolumn) AS alias FROM table GROUP BY groupcolumn`",
      "Group `payments` by `payment_method` and sum `amount` as `total`.",
    ],
    hiddenTestQuery: "SELECT payment_method, SUM(amount) AS total FROM payments GROUP BY payment_method;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Tickets per status",
    description: "For each status, show how many tickets are currently in it. Name the aggregate column `count`.",
    difficulty: "junior",
    category: "Aggregation",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT status, COUNT(*) AS count FROM tickets GROUP BY status;",
    tags: ["Aggregation", "GROUP BY", "COUNT"],
    hints: [
      "You want to split the data into groups based on a column and count per group.",
      "`GROUP BY column` groups the rows, `COUNT(*)` counts per group: `SELECT column, COUNT(*) AS count FROM table GROUP BY column`",
      "Group `tickets` by `status` and count with `COUNT(*)` as `count`.",
    ],
    hiddenTestQuery: "SELECT status, COUNT(*) AS count FROM tickets GROUP BY status;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("agg", {
    title: "Reviews higher than average (subquery)",
    description: "Show all reviews that are better than the average rating of all movies.",
    difficulty: "intermediate",
    category: "Aggregation",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT * FROM reviews WHERE stars > (SELECT AVG(stars) FROM reviews);",
    tags: ["Aggregation", "Subquery", "AVG"],
    hints: [
      "You want to filter rows whose value is above a calculated average — you can use a subquery in the WHERE clause for this.",
      "A subquery in WHERE calculates the comparison value: `WHERE column > (SELECT AVG(column) FROM table)`",
      "Use a subquery: `WHERE stars > (SELECT ...)` with `AVG(stars)` from the same table.",
    ],
    hiddenTestQuery: "SELECT * FROM reviews WHERE stars > (SELECT AVG(stars) FROM reviews);",
    hiddenTestMode: "rows",
  })
);
