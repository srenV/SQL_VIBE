/**
 * JOIN-Uebungen.
 * Enthaelt Uebungen fuer INNER JOIN, LEFT JOIN und RIGHT JOIN.
 */
import { makeWriteExercise, makeDebugExercise, makePredictExercise, makeSchemaExercise, resetCounter } from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";

export const joinExercises: Exercise[] = [];
resetCounter();
joinExercises.push(
  makeWriteExercise("joi", {
    title: "kunden und bestellungen verbinden",
    description: "Verbinde `kunden` und `bestellungen` mit einem INNER JOIN. Gib den Kundennamen und den Gesamtbetrag der Bestellung aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, gesamtbetrag FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id`",
    ],
    hiddenTestQuery: `SELECT name, gesamtbetrag FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen kunden und bestellungen",
    description: "Gib den Namen jedes Kunden und den Gesamtbetrag seiner Bestellungen aus. Auch Kunden ohne Bestellung sollen mit ihrem Namen in der Ausgabe erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, gesamtbetrag FROM kunden LEFT JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.id = rechte.fremdschluessel`",
      "Konkret: `FROM kunden LEFT JOIN bestellungen ON kunden.id = bestellungen.kunde_id`",
    ],
    hiddenTestQuery: `SELECT name, gesamtbetrag FROM kunden LEFT JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen kunden und bestellungen",
    description: "Gib den Kundennamen und den Gesamtbetrag jeder Bestellung aus. Auch Bestellungen ohne zugehoerigen Kundeneintrag sollen mit ihrem Gesamtbetrag erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT name, gesamtbetrag FROM kunden RIGHT JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.id = rechte.fremdschluessel`",
      "Konkret: `FROM kunden RIGHT JOIN bestellungen ON kunden.id = bestellungen.kunde_id`",
    ],
    hiddenTestQuery: `SELECT name, gesamtbetrag FROM kunden RIGHT JOIN bestellungen ON kunden.id = bestellungen.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Bestellungen pro Kunde zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Bestellungen jeder Kunde aufgegeben hat. Gib die Kunden-ID und die Anzahl der Bestellungen pro Kunde aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id GROUP BY kunden.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id GROUP BY kunden.id`",
    ],
    hiddenTestQuery: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN bestellungen ON kunden.id = bestellungen.kunde_id GROUP BY kunden.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "produkte und kategorien verbinden",
    description: "Verbinde `produkte` und `kategorien` mit einem INNER JOIN. Gib den Produktnamen und den Kategorienamen aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT produkte.name, kategorien.name FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.fremdschluessel = tabelle2.schluessel`",
      "Konkret: `FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT produkte.name, kategorien.name FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen produkte und kategorien",
    description: "Gib den Namen jedes Produkts und den Namen seiner Kategorie aus. Auch Produkte ohne zugeordnete Kategorie sollen mit ihrem Produktnamen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT produkte.name, kategorien.name FROM produkte LEFT JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM produkte LEFT JOIN kategorien ON produkte.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT produkte.name, kategorien.name FROM produkte LEFT JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen produkte und kategorien",
    description: "Gib den Produktnamen und den Kategorienamen aus. Auch Kategorien ohne zugeordnetes Produkt sollen mit ihrem Kategorienamen erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT produkte.name, kategorien.name FROM produkte RIGHT JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM produkte RIGHT JOIN kategorien ON produkte.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT produkte.name, kategorien.name FROM produkte RIGHT JOIN kategorien ON produkte.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Kategorien pro Produkt zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Kategorien jedem Produkt zugeordnet sind. Gib die Produkt-ID und die Anzahl der Kategorien pro Produkt aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT produkte.id, COUNT(*) AS anzahl FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id GROUP BY produkte.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT produkte.id, COUNT(*) AS anzahl FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id GROUP BY produkte.id`",
    ],
    hiddenTestQuery: `SELECT produkte.id, COUNT(*) AS anzahl FROM produkte INNER JOIN kategorien ON produkte.kategorie_id = kategorien.id GROUP BY produkte.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "bestellungen und zahlungen verbinden",
    description: "Verbinde `bestellungen` und `zahlungen` mit einem INNER JOIN. Gib den Bestellstatus und den Zahlungsbetrag aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, betrag FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id`",
    ],
    hiddenTestQuery: `SELECT status, betrag FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen bestellungen und zahlungen",
    description: "Gib den Status jeder Bestellung und den Betrag der zugehoerigen Zahlung aus. Auch Bestellungen ohne Zahlung sollen mit ihrem Status erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, betrag FROM bestellungen LEFT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM bestellungen LEFT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id`",
    ],
    hiddenTestQuery: `SELECT status, betrag FROM bestellungen LEFT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen bestellungen und zahlungen",
    description: "Gib den Bestellstatus und den Zahlungsbetrag aus. Auch Zahlungen ohne zugehoerige Bestellung sollen mit ihrem Betrag erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT status, betrag FROM bestellungen RIGHT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM bestellungen RIGHT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id`",
    ],
    hiddenTestQuery: `SELECT status, betrag FROM bestellungen RIGHT JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Zahlungen pro Bestellung zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Zahlungen jeder Bestellung zugeordnet sind. Gib die Bestell-ID und die Anzahl der Zahlungen pro Bestellung aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "shop",
    referenceQuery: `SELECT bestellungen.id, COUNT(*) AS anzahl FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id GROUP BY bestellungen.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT bestellungen.id, COUNT(*) AS anzahl FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id GROUP BY bestellungen.id`",
    ],
    hiddenTestQuery: `SELECT bestellungen.id, COUNT(*) AS anzahl FROM bestellungen INNER JOIN zahlungen ON bestellungen.id = zahlungen.bestellung_id GROUP BY bestellungen.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "nutzer und workouts verbinden",
    description: "Verbinde `nutzer` und `workouts` mit einem INNER JOIN. Gib den Nutzernamen und die Dauer in Minuten aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT nutzer.name, dauer_min FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, dauer_min FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen nutzer und workouts",
    description: "Gib den Namen jedes Nutzers und die Dauer seiner Workouts in Minuten aus. Auch Nutzer ohne Workouts sollen mit ihrem Namen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT nutzer.name, dauer_min FROM nutzer LEFT JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM nutzer LEFT JOIN workouts ON nutzer.id = workouts.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, dauer_min FROM nutzer LEFT JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen nutzer und workouts",
    description: "Gib den Nutzernamen und die Workout-Dauer in Minuten aus. Auch Workouts ohne zugeordneten Nutzer sollen mit ihrer Dauer erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT nutzer.name, dauer_min FROM nutzer RIGHT JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM nutzer RIGHT JOIN workouts ON nutzer.id = workouts.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, dauer_min FROM nutzer RIGHT JOIN workouts ON nutzer.id = workouts.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Workouts pro Nutzer zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Workouts jeder Nutzer absolviert hat. Gib die Nutzer-ID und die Anzahl der Workouts pro Nutzer aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id GROUP BY nutzer.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id GROUP BY nutzer.id`",
    ],
    hiddenTestQuery: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN workouts ON nutzer.id = workouts.nutzer_id GROUP BY nutzer.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "workouts und saetze verbinden",
    description: "Verbinde `workouts` und `saetze` mit einem INNER JOIN. Gib das Workout-Datum und die Anzahl der Wiederholungen aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT datum, wiederholungen FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id`",
    ],
    hiddenTestQuery: `SELECT datum, wiederholungen FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen workouts und saetze",
    description: "Gib das Datum jedes Workouts und die Wiederholungszahlen der zugehoerigen Saetze aus. Auch Workouts ohne Saetze sollen mit ihrem Datum erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT datum, wiederholungen FROM workouts LEFT JOIN saetze ON workouts.id = saetze.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM workouts LEFT JOIN saetze ON workouts.id = saetze.workout_id`",
    ],
    hiddenTestQuery: `SELECT datum, wiederholungen FROM workouts LEFT JOIN saetze ON workouts.id = saetze.workout_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen workouts und saetze",
    description: "Gib das Workout-Datum und die Wiederholungszahl jedes Satzes aus. Auch Saetze ohne zugeordnetes Workout sollen mit ihren Wiederholungen erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT datum, wiederholungen FROM workouts RIGHT JOIN saetze ON workouts.id = saetze.workout_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM workouts RIGHT JOIN saetze ON workouts.id = saetze.workout_id`",
    ],
    hiddenTestQuery: `SELECT datum, wiederholungen FROM workouts RIGHT JOIN saetze ON workouts.id = saetze.workout_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Saetze pro Workout zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Saetze jedes Workout enthaelt. Gib die Workout-ID und die Anzahl der Saetze pro Workout aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "fitness",
    referenceQuery: `SELECT workouts.id, COUNT(*) AS anzahl FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id GROUP BY workouts.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT workouts.id, COUNT(*) AS anzahl FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id GROUP BY workouts.id`",
    ],
    hiddenTestQuery: `SELECT workouts.id, COUNT(*) AS anzahl FROM workouts INNER JOIN saetze ON workouts.id = saetze.workout_id GROUP BY workouts.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "mitarbeiter und abteilungen verbinden",
    description: "Verbinde `mitarbeiter` und `abteilungen` mit einem INNER JOIN. Gib den Mitarbeiternamen und den Abteilungsnamen aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.fremdschluessel = tabelle2.schluessel`",
      "Konkret: `FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id`",
    ],
    hiddenTestQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen mitarbeiter und abteilungen",
    description: "Gib den Namen jedes Mitarbeiters und den Namen seiner Abteilung aus. Auch Mitarbeiter ohne zugeordnete Abteilung sollen mit ihrem Namen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter LEFT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM mitarbeiter LEFT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id`",
    ],
    hiddenTestQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter LEFT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen mitarbeiter und abteilungen",
    description: "Gib den Mitarbeiternamen und den Abteilungsnamen aus. Auch Abteilungen ohne zugeordnete Mitarbeiter sollen mit ihrem Abteilungsnamen erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter RIGHT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM mitarbeiter RIGHT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id`",
    ],
    hiddenTestQuery: `SELECT mitarbeiter.name, abteilungen.name FROM mitarbeiter RIGHT JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Abteilungen pro Mitarbeiter zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie vielen Abteilungen jeder Mitarbeiter angehoert. Gib die Mitarbeiter-ID und die Anzahl der Abteilungen pro Mitarbeiter aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id GROUP BY mitarbeiter.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id GROUP BY mitarbeiter.id`",
    ],
    hiddenTestQuery: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN abteilungen ON mitarbeiter.abteilung_id = abteilungen.id GROUP BY mitarbeiter.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "mitarbeiter und urlaub verbinden",
    description: "Verbinde `mitarbeiter` und `urlaub` mit einem INNER JOIN. Gib den Mitarbeiternamen und die Anzahl der Urlaubstage aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, tage FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id`",
    ],
    hiddenTestQuery: `SELECT name, tage FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen mitarbeiter und urlaub",
    description: "Gib den Namen jedes Mitarbeiters und die Anzahl seiner gebuchten Urlaubstage aus. Auch Mitarbeiter ohne Urlaubseintrag sollen mit ihrem Namen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, tage FROM mitarbeiter LEFT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM mitarbeiter LEFT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id`",
    ],
    hiddenTestQuery: `SELECT name, tage FROM mitarbeiter LEFT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen mitarbeiter und urlaub",
    description: "Gib den Mitarbeiternamen und die Anzahl der Urlaubstage aus. Auch Urlaubseintraege ohne zugehoerigen Mitarbeiter sollen mit ihrer Tagesanzahl erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT name, tage FROM mitarbeiter RIGHT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM mitarbeiter RIGHT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id`",
    ],
    hiddenTestQuery: `SELECT name, tage FROM mitarbeiter RIGHT JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Urlaub pro Mitarbeiter zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Urlaubseintraege jeder Mitarbeiter hat. Gib die Mitarbeiter-ID und die Anzahl der Eintraege pro Mitarbeiter aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "hr",
    referenceQuery: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id GROUP BY mitarbeiter.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id GROUP BY mitarbeiter.id`",
    ],
    hiddenTestQuery: `SELECT mitarbeiter.id, COUNT(*) AS anzahl FROM mitarbeiter INNER JOIN urlaub ON mitarbeiter.id = urlaub.mitarbeiter_id GROUP BY mitarbeiter.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "tickets und agenten verbinden",
    description: "Verbinde `tickets` und `agenten` mit einem INNER JOIN. Gib den Ticket-Titel und den Agentennamen aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, agenten.name FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.fremdschluessel = tabelle2.schluessel`",
      "Konkret: `FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, agenten.name FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen tickets und agenten",
    description: "Gib den Titel jedes Tickets und den Namen des zustaendigen Agenten aus. Auch Tickets ohne zugewiesenen Agenten sollen mit ihrem Titel erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, agenten.name FROM tickets LEFT JOIN agenten ON tickets.agent_id = agenten.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM tickets LEFT JOIN agenten ON tickets.agent_id = agenten.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, agenten.name FROM tickets LEFT JOIN agenten ON tickets.agent_id = agenten.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen tickets und agenten",
    description: "Gib den Ticket-Titel und den Agentennamen aus. Auch Agenten ohne zugewiesene Tickets sollen mit ihrem Namen erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, agenten.name FROM tickets RIGHT JOIN agenten ON tickets.agent_id = agenten.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM tickets RIGHT JOIN agenten ON tickets.agent_id = agenten.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, agenten.name FROM tickets RIGHT JOIN agenten ON tickets.agent_id = agenten.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Agenten pro Ticket zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Agenten jedem Ticket zugeordnet sind. Gib die Ticket-ID und die Anzahl der Agenten pro Ticket aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id GROUP BY tickets.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id GROUP BY tickets.id`",
    ],
    hiddenTestQuery: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN agenten ON tickets.agent_id = agenten.id GROUP BY tickets.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "tickets und kategorien verbinden",
    description: "Verbinde `tickets` und `kategorien` mit einem INNER JOIN. Gib den Ticket-Titel und den Kategorienamen aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, kategorien.name FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.fremdschluessel = tabelle2.schluessel`",
      "Konkret: `FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, kategorien.name FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen tickets und kategorien",
    description: "Gib den Titel jedes Tickets und den Namen seiner Kategorie aus. Auch Tickets ohne Kategorie sollen mit ihrem Titel erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, kategorien.name FROM tickets LEFT JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM tickets LEFT JOIN kategorien ON tickets.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, kategorien.name FROM tickets LEFT JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen tickets und kategorien",
    description: "Gib den Ticket-Titel und den Kategorienamen aus. Auch Kategorien ohne zugeordnete Tickets sollen mit ihrem Kategorienamen erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.titel, kategorien.name FROM tickets RIGHT JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.fremdschluessel = rechte.schluessel`",
      "Konkret: `FROM tickets RIGHT JOIN kategorien ON tickets.kategorie_id = kategorien.id`",
    ],
    hiddenTestQuery: `SELECT tickets.titel, kategorien.name FROM tickets RIGHT JOIN kategorien ON tickets.kategorie_id = kategorien.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Kategorien pro Ticket zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Kategorien jedem Ticket zugeordnet sind. Gib die Ticket-ID und die Anzahl der Kategorien pro Ticket aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "tickets",
    referenceQuery: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id GROUP BY tickets.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id GROUP BY tickets.id`",
    ],
    hiddenTestQuery: `SELECT tickets.id, COUNT(*) AS anzahl FROM tickets INNER JOIN kategorien ON tickets.kategorie_id = kategorien.id GROUP BY tickets.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "kunden und konten verbinden",
    description: "Verbinde `kunden` und `konten` mit einem INNER JOIN. Gib den Kundennamen und den Kontosaldo aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kunden.name, saldo FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id`",
    ],
    hiddenTestQuery: `SELECT kunden.name, saldo FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen kunden und konten",
    description: "Gib den Namen jedes Kunden und den Saldo seiner Konten aus. Auch Kunden ohne Konto sollen mit ihrem Namen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kunden.name, saldo FROM kunden LEFT JOIN konten ON kunden.id = konten.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM kunden LEFT JOIN konten ON kunden.id = konten.kunde_id`",
    ],
    hiddenTestQuery: `SELECT kunden.name, saldo FROM kunden LEFT JOIN konten ON kunden.id = konten.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen kunden und konten",
    description: "Gib den Kundennamen und den Kontosaldo aus. Auch Konten ohne zugehoerigen Kunden sollen mit ihrem Saldo erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kunden.name, saldo FROM kunden RIGHT JOIN konten ON kunden.id = konten.kunde_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM kunden RIGHT JOIN konten ON kunden.id = konten.kunde_id`",
    ],
    hiddenTestQuery: `SELECT kunden.name, saldo FROM kunden RIGHT JOIN konten ON kunden.id = konten.kunde_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Konten pro Kunde zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Konten jeder Kunde besitzt. Gib die Kunden-ID und die Anzahl der Konten pro Kunde aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id GROUP BY kunden.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id GROUP BY kunden.id`",
    ],
    hiddenTestQuery: `SELECT kunden.id, COUNT(*) AS anzahl FROM kunden INNER JOIN konten ON kunden.id = konten.kunde_id GROUP BY kunden.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "konten und transaktionen verbinden",
    description: "Verbinde `konten` und `transaktionen` mit einem INNER JOIN. Gib die Kontonummer und den Transaktionsbetrag aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kontonummer, betrag FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id`",
    ],
    hiddenTestQuery: `SELECT kontonummer, betrag FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen konten und transaktionen",
    description: "Gib die Kontonummer jedes Kontos und den Betrag der zugehoerigen Transaktionen aus. Auch Konten ohne Transaktionen sollen mit ihrer Kontonummer erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kontonummer, betrag FROM konten LEFT JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM konten LEFT JOIN transaktionen ON konten.id = transaktionen.konto_id`",
    ],
    hiddenTestQuery: `SELECT kontonummer, betrag FROM konten LEFT JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen konten und transaktionen",
    description: "Gib die Kontonummer und den Transaktionsbetrag aus. Auch Transaktionen ohne zugeordnetes Konto sollen mit ihrem Betrag erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT kontonummer, betrag FROM konten RIGHT JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM konten RIGHT JOIN transaktionen ON konten.id = transaktionen.konto_id`",
    ],
    hiddenTestQuery: `SELECT kontonummer, betrag FROM konten RIGHT JOIN transaktionen ON konten.id = transaktionen.konto_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Transaktionen pro Konto zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Transaktionen jedes Konto hat. Gib die Konto-ID und die Anzahl der Transaktionen pro Konto aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "banking",
    referenceQuery: `SELECT konten.id, COUNT(*) AS anzahl FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id GROUP BY konten.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT konten.id, COUNT(*) AS anzahl FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id GROUP BY konten.id`",
    ],
    hiddenTestQuery: `SELECT konten.id, COUNT(*) AS anzahl FROM konten INNER JOIN transaktionen ON konten.id = transaktionen.konto_id GROUP BY konten.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "nutzer und watch_history verbinden",
    description: "Verbinde `nutzer` und `watch_history` mit einem INNER JOIN. Gib den Nutzernamen und den Fortschritt in Prozent aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen nutzer und watch_history",
    description: "Gib den Namen jedes Nutzers und seinen Fortschritt in Prozent aus. Auch Nutzer ohne Watch-History sollen mit ihrem Namen erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer LEFT JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM nutzer LEFT JOIN watch_history ON nutzer.id = watch_history.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer LEFT JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "RIGHT JOIN zwischen nutzer und watch_history",
    description: "Gib den Nutzernamen und den Fortschritt in Prozent aus. Auch Watch-History-Eintraege ohne zugeordneten Nutzer sollen mit ihrem Fortschritt erscheinen.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer RIGHT JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein RIGHT JOIN gibt ALLE Zeilen der rechten (zweiten) Tabelle zurueck — das Spiegelbild des LEFT JOIN. Fehlende Werte auf der linken Seite werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle RIGHT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM nutzer RIGHT JOIN watch_history ON nutzer.id = watch_history.nutzer_id`",
    ],
    hiddenTestQuery: `SELECT nutzer.name, fortschritt_prozent FROM nutzer RIGHT JOIN watch_history ON nutzer.id = watch_history.nutzer_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "Watch-History pro Nutzer zaehlen (JOIN + GROUP BY)",
    description: "Zaehle, wie viele Eintraege jeder Nutzer in der Watch-History hat. Gib die Nutzer-ID und die Anzahl der Eintraege pro Nutzer aus.",
    difficulty: "intermediate",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id GROUP BY nutzer.id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Verbinde zuerst die Tabellen mit einem JOIN, dann gruppiere die Ergebniszeilen nach einer Spalte, um pro Gruppe zu zaehlen.",
      "Kombiniere JOIN mit `GROUP BY spalte` und `COUNT(*) AS alias`.",
      "Struktur: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id GROUP BY nutzer.id`",
    ],
    hiddenTestQuery: `SELECT nutzer.id, COUNT(*) AS anzahl FROM nutzer INNER JOIN watch_history ON nutzer.id = watch_history.nutzer_id GROUP BY nutzer.id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "filme und bewertungen verbinden",
    description: "Verbinde `filme` und `bewertungen` mit einem INNER JOIN. Gib den Filmtitel und die Sternebewertung aus.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT filme.titel, sterne FROM filme INNER JOIN bewertungen ON filme.id = bewertungen.film_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein INNER JOIN verbindet zwei Tabellen und gibt nur Zeilen zurueck, die in BEIDEN Tabellen einen Treffer haben.",
      "Syntax: `FROM tabelle1 INNER JOIN tabelle2 ON tabelle1.schluessel = tabelle2.fremdschluessel`",
      "Konkret: `FROM filme INNER JOIN bewertungen ON filme.id = bewertungen.film_id`",
    ],
    hiddenTestQuery: `SELECT filme.titel, sterne FROM filme INNER JOIN bewertungen ON filme.id = bewertungen.film_id;`,
    hiddenTestMode: "rows",
  }),

  makeWriteExercise("joi", {
    title: "LEFT JOIN zwischen filme und bewertungen",
    description: "Gib den Titel jedes Films und seine Sternebewertung aus. Auch Filme ohne Bewertung sollen mit ihrem Titel erscheinen.",
    difficulty: "junior",
    category: "JOIN",
    datasetId: "streaming",
    referenceQuery: `SELECT filme.titel, sterne FROM filme LEFT JOIN bewertungen ON filme.id = bewertungen.film_id;`,
    expectedResultText: "",
    tags: ["JOIN", "INNER JOIN", "LEFT JOIN"],
    hints: [
      "Ein LEFT JOIN gibt ALLE Zeilen der linken (ersten) Tabelle zurueck — auch wenn es in der rechten Tabelle keine Uebereinstimmung gibt. Fehlende Werte werden als NULL dargestellt.",
      "Syntax: `FROM linke_tabelle LEFT JOIN rechte_tabelle ON linke.schluessel = rechte.fremdschluessel`",
      "Konkret: `FROM filme LEFT JOIN bewertungen ON filme.id = bewertungen.film_id`",
    ],
    hiddenTestQuery: `SELECT filme.titel, sterne FROM filme LEFT JOIN bewertungen ON filme.id = bewertungen.film_id;`,
    hiddenTestMode: "rows",
  })
);
