/**
 * SELECT-Grundlagen-Uebungen.
 * Enthaelt Uebungen fuer einfache bis fortgeschrittene SELECT-Abfragen.
 */
import { shopDataset } from "@/data/datasets/shop";
import { fitnessDataset } from "@/data/datasets/fitness";
import { hrDataset } from "@/data/datasets/hr";
import { ticketsDataset } from "@/data/datasets/tickets";
import { bankingDataset } from "@/data/datasets/banking";
import { streamingDataset } from "@/data/datasets/streaming";
import { logsDataset } from "@/data/datasets/logs";
import {
  makeWriteExercise,
  makeDebugExercise,
  makePredictExercise,
  makeSchemaExercise,
  resetCounter,
} from "@/data/exercises/_factory";
import type { Exercise } from "@/types/exercise";

// ============================================================
// SELECT GRUNDLAGEN (40 exercises)
// ============================================================
export const selectExercises: Exercise[] = [];

resetCounter();
selectExercises.push(
  makeWriteExercise("sel", {
    title: "Alle Kunden ausgeben",
    description: "Schreibe eine Query, die alle Spalten aller Kunden aus der Tabelle `kunden` ausgibt.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT * FROM kunden;",
    expectedResultText: "10 Zeilen: id, name, email, stadt, registriert_am",
    tags: ["SELECT", "Grundlagen"],
    hints: ["Verwende `SELECT *` um alle Spalten zu selektieren.", "Die Tabelle heisst `kunden`."],
    hiddenTestQuery: "SELECT * FROM kunden;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Namen der Produkte",
    description: "Gib nur die Spalte `name` aus der Tabelle `produkte` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name FROM produkte;",
    tags: ["SELECT", "Spalten"],
    hints: ["Verwende `SELECT name` statt `SELECT *`."],
    hiddenTestQuery: "SELECT name FROM produkte;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Name und Preis der Produkte",
    description: "Gib die Spalten `name` und `preis` aller Produkte aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name, preis FROM produkte;",
    tags: ["SELECT", "Spalten"],
    hints: ["Trenne die Spaltennamen im SELECT durch Kommas."],
    hiddenTestQuery: "SELECT name, preis FROM produkte;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Produktname mit Alias",
    description: "Gib die Spalte `name` aus der Tabelle `produkte` aus, aber benenne sie im Ergebnis in `produkt_name` um.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name AS produkt_name FROM produkte;",
    tags: ["SELECT", "Alias"],
    hints: ["Verwende `AS` um einen Alias zu vergeben.", "Beispiel: `SELECT spalte AS alias`"],
    hiddenTestQuery: "SELECT name AS produkt_name FROM produkte;",
    hiddenTestMode: "columns",
  }),
  makeWriteExercise("sel", {
    title: "Doppelte Staedte finden (DISTINCT)",
    description: "Gib alle eindeutigen Staedte aus der Tabelle `kunden` aus. Verwende `DISTINCT`.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT DISTINCT stadt FROM kunden;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["`DISTINCT` entfernt doppelte Werte.", "Syntax: `SELECT DISTINCT spalte`"],
    hiddenTestQuery: "SELECT DISTINCT stadt FROM kunden;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Abteilungen anzeigen",
    description: "Gib alle Spalten der Tabelle `abteilungen` aus dem HR-Datensatz aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT * FROM abteilungen;",
    tags: ["SELECT", "Grundlagen"],
    hints: ["Die Tabelle heisst `abteilungen`."],
    hiddenTestQuery: "SELECT * FROM abteilungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Mitarbeiternamen und Positionen",
    description: "Gib die Spalten `name` und `position` aller Mitarbeiter aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, position FROM mitarbeiter;",
    tags: ["SELECT", "Spalten"],
    hints: ["Verwende `SELECT name, position`."],
    hiddenTestQuery: "SELECT name, position FROM mitarbeiter;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Agenten-Teams",
    description: "Gib alle eindeutigen Werte der Spalte `team` aus der Tabelle `agenten` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT DISTINCT team FROM agenten;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["`DISTINCT` entfernt Duplikate aus dem Ergebnis."],
    hiddenTestQuery: "SELECT DISTINCT team FROM agenten;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Nutzer-Abonnements",
    description: "Gib die Spalten `name` und `abonnement` aus der Tabelle `nutzer` des Streaming-Datensatzes aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT name, abonnement FROM nutzer;",
    tags: ["SELECT", "Spalten"],
    hints: ["Nutze den Streaming-Datensatz, Tabelle `nutzer`."],
    hiddenTestQuery: "SELECT name, abonnement FROM nutzer;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Uebungsnamen mit Alias",
    description: "Gib die Spalte `name` aus der Tabelle `uebungen` aus und benenne sie als `uebung`.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT name AS uebung FROM uebungen;",
    tags: ["SELECT", "Alias"],
    hints: ["Verwende `AS` fuer den Alias."],
    hiddenTestQuery: "SELECT name AS uebung FROM uebungen;",
    hiddenTestMode: "columns",
  })
);

// More SELECT exercises 11-20
selectExercises.push(
  makeWriteExercise("sel", {
    title: "Alle Ticket-Titel",
    description: "Gib nur die Spalte `titel` aller Tickets aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT titel FROM tickets;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `tickets`, die Spalte `titel`."],
    hiddenTestQuery: "SELECT titel FROM tickets;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Kontonummern und Salden",
    description: "Gib die Spalten `kontonummer` und `saldo` aus der Tabelle `konten` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT kontonummer, saldo FROM konten;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `konten`."],
    hiddenTestQuery: "SELECT kontonummer, saldo FROM konten;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Filmtitel und Genre",
    description: "Gib die Spalten `titel` und `genre` aus der Tabelle `filme` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT titel, genre FROM filme;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `filme`."],
    hiddenTestQuery: "SELECT titel, genre FROM filme;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Eindeutige Event-Typen",
    description: "Gib alle eindeutigen `event_typ`-Werte aus der Tabelle `events` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT DISTINCT event_typ FROM events;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT`."],
    hiddenTestQuery: "SELECT DISTINCT event_typ FROM events;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Mitarbeiter mit vollem Namen",
    description: "Gib alle Spalten der Tabelle `mitarbeiter` aus, aber benenne `name` als `vollstaendiger_name`.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT id, name AS vollstaendiger_name, abteilung_id, position, gehalt, einstiegsdatum, manager_id FROM mitarbeiter;",
    tags: ["SELECT", "Alias"],
    hints: ["Mische `*` nicht mit Alias – liste stattdessen alle Spalten auf."],
    hiddenTestQuery: "SELECT id, name AS vollstaendiger_name, abteilung_id, position, gehalt, einstiegsdatum, manager_id FROM mitarbeiter;",
    hiddenTestMode: "columns",
  }),
  makeWriteExercise("sel", {
    title: "Produktkategorien ohne Duplikate",
    description: "Gib alle eindeutigen Kategorienamen aus der Tabelle `kategorien` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT DISTINCT name FROM kategorien;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT name`."],
    hiddenTestQuery: "SELECT DISTINCT name FROM kategorien;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Tickets mit Beschreibung",
    description: "Gib die Spalten `titel` und `beschreibung` aller Tickets aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT titel, beschreibung FROM tickets;",
    tags: ["SELECT", "Spalten"],
    hints: ["Verwende `SELECT titel, beschreibung`."],
    hiddenTestQuery: "SELECT titel, beschreibung FROM tickets;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Namen und Geburtsdaten der Bankkunden",
    description: "Gib die Spalten `name` und `geburtsdatum` aus der Tabelle `kunden` im Banking-Datensatz aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT name, geburtsdatum FROM kunden;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `kunden`."],
    hiddenTestQuery: "SELECT name, geburtsdatum FROM kunden;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Uebungskategorien",
    description: "Gib die Spalten `name` und `muskelgruppe` aus der Tabelle `uebungen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT name, muskelgruppe FROM uebungen;",
    tags: ["SELECT", "Spalten"],
    hints: ["Tabelle `uebungen`, Spalten `name` und `muskelgruppe`."],
    hiddenTestQuery: "SELECT name, muskelgruppe FROM uebungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Session-IDs und Browser",
    description: "Gib die Spalten `id` und `browser` aus der Tabelle `sessions` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT id, browser FROM sessions;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `sessions`."],
    hiddenTestQuery: "SELECT id, browser FROM sessions;",
    hiddenTestMode: "rows",
  })
);

// SELECT exercises 21-30
selectExercises.push(
  makeWriteExercise("sel", {
    title: "Eindeutige Abonnement-Typen",
    description: "Gib alle eindeutigen `abonnement`-Werte aus der Tabelle `nutzer` des Streaming-Datensatzes aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT DISTINCT abonnement FROM nutzer;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT abonnement`."],
    hiddenTestQuery: "SELECT DISTINCT abonnement FROM nutzer;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Transaktionstypen",
    description: "Gib alle eindeutigen Werte der Spalte `typ` aus der Tabelle `transaktionen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT DISTINCT typ FROM transaktionen;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT`."],
    hiddenTestQuery: "SELECT DISTINCT typ FROM transaktionen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Produktname und Lagerbestand mit Alias",
    description: "Gib `name` als `produkt` und `lagerbestand` als `auf_lager` aus der Tabelle `produkte` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name AS produkt, lagerbestand AS auf_lager FROM produkte;",
    tags: ["SELECT", "Alias"],
    hints: ["Verwende `AS` fuer beide Spalten."],
    hiddenTestQuery: "SELECT name AS produkt, lagerbestand AS auf_lager FROM produkte;",
    hiddenTestMode: "columns",
  }),
  makeWriteExercise("sel", {
    title: "Alle Ticket-Statuswerte",
    description: "Gib alle eindeutigen `status`-Werte aus der Tabelle `tickets` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT DISTINCT status FROM tickets;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT status`."],
    hiddenTestQuery: "SELECT DISTINCT status FROM tickets;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Filmtitel und Erscheinungsjahr",
    description: "Gib die Spalten `titel` und `jahr` aus der Tabelle `filme` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT titel, jahr FROM filme;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `filme`."],
    hiddenTestQuery: "SELECT titel, jahr FROM filme;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Workout-Daten",
    description: "Gib alle Spalten der Tabelle `workouts` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT * FROM workouts;",
    tags: ["SELECT", "Grundlagen"],
    hints: ["Verwende `SELECT *`."],
    hiddenTestQuery: "SELECT * FROM workouts;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Kontotypen ohne Duplikate",
    description: "Gib alle eindeutigen `typ`-Werte aus der Tabelle `konten` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT DISTINCT typ FROM konten;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT typ`."],
    hiddenTestQuery: "SELECT DISTINCT typ FROM konten;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Event-Seiten anzeigen",
    description: "Gib die Spalte `seite` aus der Tabelle `events` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT seite FROM events;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `events`."],
    hiddenTestQuery: "SELECT seite FROM events;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Bewerbungsstatus ohne Duplikate",
    description: "Gib alle eindeutigen `status`-Werte aus der Tabelle `bewerbungen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT DISTINCT status FROM bewerbungen;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT status`."],
    hiddenTestQuery: "SELECT DISTINCT status FROM bewerbungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Zahlungsmittel",
    description: "Gib alle eindeutigen `zahlungsmittel`-Werte aus der Tabelle `zahlungen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT DISTINCT zahlungsmittel FROM zahlungen;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT zahlungsmittel`."],
    hiddenTestQuery: "SELECT DISTINCT zahlungsmittel FROM zahlungen;",
    hiddenTestMode: "rows",
  })
);

// SELECT exercises 31-40
selectExercises.push(
  makeWriteExercise("sel", {
    title: "Namen und Staedte mit Alias",
    description: "Gib `name` als `kunde` und `stadt` aus der Tabelle `kunden` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: shopDataset.id,
    referenceQuery: "SELECT name AS kunde, stadt FROM kunden;",
    tags: ["SELECT", "Alias"],
    hints: ["Verwende `AS` fuer den Alias."],
    hiddenTestQuery: "SELECT name AS kunde, stadt FROM kunden;",
    hiddenTestMode: "columns",
  }),
  makeWriteExercise("sel", {
    title: "Alle Ticket-Prioritaeten",
    description: "Gib alle eindeutigen `prioritaet`-Werte aus der Tabelle `tickets` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: ticketsDataset.id,
    referenceQuery: "SELECT DISTINCT prioritaet FROM tickets;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT prioritaet`."],
    hiddenTestQuery: "SELECT DISTINCT prioritaet FROM tickets;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Uebungsname und Kategorie",
    description: "Gib `name` und `kategorie` aus der Tabelle `uebungen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT name, kategorie FROM uebungen;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `uebungen`."],
    hiddenTestQuery: "SELECT name, kategorie FROM uebungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Filmgenre und Dauer",
    description: "Gib `genre` und `dauer_min` aus der Tabelle `filme` aus und benenne `dauer_min` als `dauer_minuten`.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: streamingDataset.id,
    referenceQuery: "SELECT genre, dauer_min AS dauer_minuten FROM filme;",
    tags: ["SELECT", "Alias"],
    hints: ["Verwende `AS` fuer den Alias."],
    hiddenTestQuery: "SELECT genre, dauer_min AS dauer_minuten FROM filme;",
    hiddenTestMode: "columns",
  }),
  makeWriteExercise("sel", {
    title: "Alle Fehlercodes",
    description: "Gib alle eindeutigen `fehlercode`-Werte aus der Tabelle `fehler` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: logsDataset.id,
    referenceQuery: "SELECT DISTINCT fehlercode FROM fehler;",
    tags: ["SELECT", "DISTINCT"],
    hints: ["Verwende `DISTINCT fehlercode`."],
    hiddenTestQuery: "SELECT DISTINCT fehlercode FROM fehler;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Bewerbungsnamen und E-Mail",
    description: "Gib `name` und `email` aus der Tabelle `bewerbungen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: hrDataset.id,
    referenceQuery: "SELECT name, email FROM bewerbungen;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `bewerbungen`."],
    hiddenTestQuery: "SELECT name, email FROM bewerbungen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Alle Kontonummern",
    description: "Gib die Spalte `kontonummer` aus der Tabelle `konten` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT kontonummer FROM konten;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `konten`."],
    hiddenTestQuery: "SELECT kontonummer FROM konten;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Transaktionsbetraege und Beschreibungen",
    description: "Gib `betrag` und `beschreibung` aus der Tabelle `transaktionen` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: bankingDataset.id,
    referenceQuery: "SELECT betrag, beschreibung FROM transaktionen;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `transaktionen`."],
    hiddenTestQuery: "SELECT betrag, beschreibung FROM transaktionen;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Workout-Dauer und Kalorien",
    description: "Gib `dauer_min` und `kalorien_verbrannt` aus der Tabelle `workouts` aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT dauer_min, kalorien_verbrannt FROM workouts;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `workouts`."],
    hiddenTestQuery: "SELECT dauer_min, kalorien_verbrannt FROM workouts;",
    hiddenTestMode: "rows",
  }),
  makeWriteExercise("sel", {
    title: "Nutzer mit Registrierungsdatum",
    description: "Gib `name` und `registriert_am` aus der Tabelle `nutzer` des Fitness-Datensatzes aus.",
    difficulty: "beginner",
    category: "SELECT",
    datasetId: fitnessDataset.id,
    referenceQuery: "SELECT name, registriert_am FROM nutzer;",
    tags: ["SELECT", "Spalten"],
    hints: ["Die Tabelle heisst `nutzer` im Fitness-Datensatz."],
    hiddenTestQuery: "SELECT name, registriert_am FROM nutzer;",
    hiddenTestMode: "rows",
  })
);
