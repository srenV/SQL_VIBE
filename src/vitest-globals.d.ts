/**
 * Globale Typdeklarationen fuer Vitest-Test-Globals.
 *
 * Ermöglicht die Verwendung von `describe`, `it`, `expect`, `vi`, `beforeEach`
 * usw. ohne expliziten Import in Testdateien.
 *
 * English: Global type declarations for Vitest test globals.
 * Allows `describe`, `it`, `expect`, `vi`, `beforeEach`, etc.
 * to be used without import in test files.
 */

import "vitest/globals";
